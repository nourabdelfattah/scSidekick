% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cellchat.R
\name{CompareCellChat}
\alias{CompareCellChat}
\title{Compare CellChat objects across groups for all shared pathways}
\usage{
CompareCellChat(
  cellchat_list,
  output_dir,
  groups.show = NULL,
  group.merged = NULL,
  colors = NULL,
  species = "human",
  thresh = 0.05
)
}
\arguments{
\item{cellchat_list}{Named list. Each element is either a CellChat object
or a character path to an RDS file. Names are used as panel titles.}

\item{output_dir}{Character. Directory for comparison PDFs.}

\item{groups.show}{Character vector or \code{NULL}. Cell identities to include.
\code{NULL} uses all shared identities.}

\item{group.merged}{Named character vector or \code{NULL}. Maps identity names to
broader categories for chord group aggregation. Names must match
\code{groups.show}.}

\item{colors}{Named character vector mapping identity names to colors.}

\item{species}{Character. \code{"human"} or \code{"mouse"}. Default \code{"human"}.}

\item{thresh}{Numeric. P-value threshold applied to individual ligand-receptor
interactions when drawing circle, chord, and heatmap plots. Default \code{0.05}
(matches CellChat's standard significance filter). Set \code{thresh = 1} to
display \strong{all} predicted interactions regardless of statistical
significance — this is essential for comparing pathways that are active
in some conditions but did not reach pathway-level significance in others
(i.e. the pathway is in \code{cc@net$prob} but not in \code{cc@netP$pathways}).
Internally, all visualisations use the bundled \code{netVisual_aggregate2}-style
functions which read \code{cc@net$prob} directly so the comparison is not
limited to pathways that passed CellChat's aggregation threshold.}
}
\value{
A named list of the (possibly updated) CellChat objects.
}
\description{
Takes a named list of CellChat objects (or paths to RDS files) and for
each pathway present in any object, generates a multi-panel comparison PDF
with circle, chord, and heatmap layouts.
}
