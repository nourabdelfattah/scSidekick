% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pathway_leadingedge.R
\name{VisualizeLeadingEdge}
\alias{VisualizeLeadingEdge}
\title{Visualize Leading-Edge Genes from GSEA Results}
\usage{
VisualizeLeadingEdge(
  seurat_object,
  gsea_files,
  search_terms = NULL,
  pathways = NULL,
  group.by,
  split.by = NULL,
  assay = "RNA",
  layer = "data",
  min_expr = 0.05,
  subset_cells = FALSE,
  subset_by = NULL,
  subset_values = NULL,
  deg_df = NULL,
  deg_gene_column = "feature",
  deg_group_column = "group",
  deg_fc_column = "logFC",
  deg_padj_column = "padj",
  deg_padj_cutoff = 0.05,
  deg_top_n = 20,
  add_dotplot = TRUE,
  add_violin = TRUE,
  add_boxplots = TRUE,
  add_pvalues = TRUE,
  add_feature_maps = FALSE,
  heatmap_type = c("cell", "group"),
  heatmap_column_split = NULL,
  show_pathway_annotation = TRUE,
  group_colors = NULL,
  split_colors = NULL,
  heatmap_params = list(row_names_side = "left", show_row_dend = FALSE,
    row_names_max_width = grid::unit(15, "cm")),
  output_dir,
  object_name = "Analysis",
  subset_name = ""
)
}
\arguments{
\item{seurat_object}{A Seurat object.}

\item{gsea_files}{Character vector of CSV file paths (output of
\code{RunGSEA} or \code{RunGSEA_pseudobulk}). Must each contain
\code{pathway} and \code{leadingEdge} columns.}

\item{search_terms}{Keyword(s) to filter pathways. See \emph{Pathway search}
above.}

\item{pathways}{Exact pathway names to select (alternative to
\code{search_terms}).}

\item{group.by}{Metadata column for the primary grouping (required). Used for
column annotation, dotplot, and violin grouping.}

\item{split.by}{Optional second metadata column for column annotation and
violin splitting.}

\item{assay}{Assay to pull expression from (default \code{"RNA"}).}

\item{layer}{Layer to pull expression from (default \code{"data"}).}

\item{min_expr}{Genes whose row-mean expression is below this threshold are
excluded from the heatmap (default \code{0.05}).}

\item{subset_cells}{Logical; if \code{TRUE}, subset \code{seurat_object} to
\code{subset_values} in \code{subset_by} before analysis.}

\item{subset_by}{Metadata column used for subsetting (requires
\code{subset_cells = TRUE}).}

\item{subset_values}{Character vector of values to retain in \code{subset_by}.}

\item{deg_df}{Optional DEG data frame (e.g. from presto or
\code{FindMarkers}). When provided, leading-edge genes are intersected with
significant DEGs and additional plots are generated.}

\item{deg_gene_column}{Column in \code{deg_df} with gene names (default
\code{"feature"}).}

\item{deg_group_column}{Column in \code{deg_df} with group labels (default
\code{"group"}).}

\item{deg_fc_column}{Column in \code{deg_df} with log-fold change (default
\code{"logFC"}).}

\item{deg_padj_column}{Column in \code{deg_df} with adjusted p-value (default
\code{"padj"}).}

\item{deg_padj_cutoff}{Adjusted p-value cut-off (default \code{0.05}).}

\item{deg_top_n}{Top N DEGs per group (by logFC) for bonus plots (default
\code{20}).}

\item{add_dotplot}{Logical; produce a \code{Seurat::DotPlot} of top DEGs
(default \code{TRUE}; requires \code{deg_df}).}

\item{add_violin}{Logical; produce stacked violin plots of top DEGs (default
\code{TRUE}; requires \code{deg_df}).}

\item{add_boxplots}{Logical; produce boxplots of top DEGs (default
\code{TRUE}; requires \code{deg_df}).}

\item{add_pvalues}{Logical; overlay Kruskal-Wallis p-value on boxplots via
\code{ggpubr::stat_compare_means} (default \code{TRUE}).}

\item{add_feature_maps}{Logical; produce per-gene \code{FeaturePlot} PDFs
for the top DEG genes (default \code{FALSE}; can be slow for large gene
sets).}

\item{group_colors}{Named character vector of colors for \code{group.by}
levels. Auto-assigned from \code{Nour_pal("all")} if \code{NULL}.}

\item{split_colors}{Named character vector of colors for \code{split.by}
levels. Auto-assigned from \code{Nour_pal("spectrum")} if \code{NULL}.}

\item{heatmap_params}{Named list of additional arguments forwarded to
\code{ComplexHeatmap::Heatmap()}. Any key supplied here overrides the
default.}

\item{output_dir}{Directory where all output files are saved.}

\item{object_name}{Label used in output file names and subfolder (default
\code{"Analysis"}).}

\item{subset_name}{Optional label appended to output file names (default
\code{""}).}
}
\value{
Invisibly returns a named list:
\describe{
\item{\code{genes_all}}{All leading-edge genes present in the Seurat object
(after \code{min_expr} filtering).}
\item{\code{genes_top}}{Top DEG genes within the leading edge, or
\code{NULL} if no \code{deg_df} was provided.}
\item{\code{top_degs}}{Filtered DEG data frame, or \code{NULL}.}
}
}
\description{
Loads one or more GSEA result CSV files (from \code{\link{RunGSEA}} or
\code{\link{RunGSEA_pseudobulk}}), searches for pathways by keyword or exact
name, extracts the union of leading-edge genes, and produces a per-cell
expression heatmap. Optionally, if a DEG data frame is supplied, the
leading-edge genes are intersected with significant DEGs and additional
diagnostic plots are produced (a second heatmap, dotplot, stacked violins,
and boxplots).
}
\section{Pathway search}{

\describe{
\item{\code{search_terms} as a character vector}{OR logic - any term
matches, case-insensitive.}
\item{\code{search_terms} as a list of character vectors}{Each element is
an AND-group (all terms must appear in the same pathway name); results are
OR'd across elements.}
\item{\code{pathways}}{Exact pathway names (alternative to
\code{search_terms}).}
}
}

\seealso{
\code{\link{RunGSEA}}, \code{\link{RunGSEA_pseudobulk}},
\code{\link{StackedVlnPlot}}
}
