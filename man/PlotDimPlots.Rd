% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/umap_plots.R
\name{PlotDimPlots}
\alias{PlotDimPlots}
\alias{PlotGridUMAP}
\alias{PlotTrendAndUMAP}
\title{UMAP grid with optional composition trend panel}
\usage{
PlotDimPlots(
  seurat_object,
  group.by = NULL,
  split.by = NULL,
  row.by = NULL,
  number_labels = FALSE,
  show_trend = FALSE,
  show_labels = FALSE,
  label.by = NULL,
  colors = NULL,
  reduction = "umap",
  pt.size = 0.1,
  label.size = 3,
  bar.width = 0.6,
  legendnrow = 2,
  legend.side = "bottom",
  legend.direction = "horizontal",
  legendtitle = NA,
  trend_width = 1,
  split.col.levels = NULL,
  width = NULL,
  height = NULL,
  join_plots = FALSE,
  join_nrow = NULL,
  join_ncol = NULL,
  output_dir = NULL,
  object_name = "",
  subset_name = "",
  file_name = NULL,
  timestamp = FALSE,
  ...
)

PlotGridUMAP(
  seurat_object,
  group.by,
  split.by = NULL,
  row.by = NULL,
  colors = NULL,
  reduction = "umap",
  pt.size = 0.1,
  col.split = NULL,
  row.split = NULL
)

PlotTrendAndUMAP(
  seurat_object,
  group.by,
  split.by = NULL,
  row.by = NULL,
  colors = NULL,
  reduction = "umap",
  pt.size = 0.1,
  bar.width = 0.6,
  label.size = 3,
  legendnrow = 2,
  legendtitle = NA,
  trend_width = 1,
  legendmargin = NULL,
  plotwidths = NULL,
  split.col.levels = NULL,
  split.cols = NULL,
  split.rows = NULL
)
}
\arguments{
\item{seurat_object}{A Seurat object.}

\item{group.by}{Character. Metadata column for point color.}

\item{split.by}{Character. Metadata column for columns (samples/conditions).}

\item{row.by}{Character or \code{NULL}. Metadata column for rows.}

\item{number_labels}{Logical. When \code{TRUE}, both the legend and (if
\code{show_labels = TRUE}) the centroid labels use a zero-padded index
(e.g., legend "01. CellType", centroid "01") instead of the actual
group name - useful when there are many groups and alphanumeric
ordering aids interpretation, or the names are too long to label
directly on the plot. Default \code{FALSE} (legend and centroid labels
show the real group name, unnumbered).}

\item{show_trend}{Logical. Add a stacked composition trend panel to the
left. Default \code{FALSE}.}

\item{show_labels}{Logical. Overlay a label at each cluster's centroid -
the group name by default, or a zero-padded index when
\code{number_labels = TRUE}. Default \code{FALSE}.}

\item{label.by}{Character or \code{NULL}. Column for label centroids.
\code{NULL} → \code{group.by}.}

\item{colors}{Named color vector. \code{NULL} auto-resolves from
PrepObject or \code{Nour_pal}.}

\item{reduction}{Character. Reduction name (case-insensitive).}

\item{pt.size}{Numeric. Point size. Default \code{0.1}.}

\item{label.size}{Numeric. Label text size. Default \code{3}.}

\item{bar.width}{Numeric. Bar width in the trend plot. Default \code{0.6}.}

\item{legendnrow}{Integer or vector. Legend rows (when
\code{legend.direction = "horizontal"}). When \code{group.by} has multiple
variables, pass a vector positionally or named by variable. Default \code{2}.}

\item{legend.side}{Character. Where to place the legend panel: \code{"bottom"}
(default), \code{"top"}, \code{"left"}, or \code{"right"}.}

\item{legend.direction}{Character. Internal layout of legend entries:
\code{"horizontal"} (default, entries in rows) or \code{"vertical"}
(entries stacked in one column).}

\item{legendtitle}{Character or \code{NA}. Legend title.}

\item{trend_width}{Numeric. Relative width of the trend panel vs. one UMAP
column. Default \code{1}.}

\item{split.col.levels}{Character vector. Explicit ordering for
\code{split.by} levels.}

\item{width}{Numeric or \code{NULL}. Saved PDF width in inches. \code{NULL}
(default) auto-sizes to fit the panel grid and the legend.}

\item{height}{Numeric or \code{NULL}. Saved PDF height in inches. \code{NULL}
(default) auto-sizes from the number of rows plus the legend strip.}

\item{join_plots}{Logical. When \code{group.by} has more than one variable,
combine the per-variable plots into a \emph{single} figure / PDF instead of
one file each. Default \code{FALSE}.}

\item{join_nrow, join_ncol}{Integer or \code{NULL}. Grid layout for
\code{join_plots}. \code{NULL} auto-arranges into a near-square grid.}

\item{output_dir}{Character or \code{NULL}. Directory to save a PDF.}

\item{object_name}{Character. Output file-name prefix.}

\item{subset_name}{Character. Optional second prefix.}

\item{file_name}{Character or \code{NULL}. Base name (no extension) for the
saved PDF. \code{NULL} (default) auto-deduces from \code{object_name},
\code{subset_name}, \code{group.by}, \code{split.by}, and \code{row.by}.}

\item{timestamp}{Logical. When \code{TRUE}, append a
\code{_YYYYMMDD-HHMMSS} stamp to the saved file name so repeated runs are
versioned instead of overwriting the previous PDF. Default \code{FALSE}.}

\item{...}{Ignored (forward compatibility).}
}
\value{
A \code{patchwork} combined plot.  With multiple \code{group.by} and
\code{join_plots = FALSE}, a named list of plots (one per variable).
}
\description{
Produces a UMAP grid where \strong{columns} correspond to \code{split.by}
levels and \strong{rows} (optionally) to \code{row.by} levels.  No empty
panels: samples missing from a row are omitted rather than shown as blank
bordered boxes.  The optional composition trend panel
(\code{show_trend = TRUE}) is placed to the left and stacked with the same
\code{ggarrange} row structure so row heights always align.
}
\section{Functions}{
\itemize{
\item \code{PlotGridUMAP()}: Deprecated - use \code{PlotDimPlots(show_trend=FALSE)}.

\item \code{PlotTrendAndUMAP()}: Deprecated - use \code{PlotDimPlots(show_trend=TRUE)}.

}}
