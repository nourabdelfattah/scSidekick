% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/umap_plots.R
\name{PlotDimPlots}
\alias{PlotDimPlots}
\alias{PlotGridUMAP}
\alias{PlotTrendAndUMAP}
\title{UMAP grid with optional composition trend panel}
\usage{
PlotDimPlots(
  seurat_object,
  group.by = NULL,
  split.by = NULL,
  row.by = NULL,
  show_trend = FALSE,
  show_labels = FALSE,
  label_by = NULL,
  colors = NULL,
  reduction = "umap",
  pt.size = 0.1,
  label.size = 3,
  bar.width = 0.6,
  legendnrow = 2,
  legendtitle = NA,
  trend_width = 1,
  split.col.levels = NULL,
  output_dir = NULL,
  object_name = "",
  subset_name = "",
  ...
)

PlotGridUMAP(
  seurat_object,
  group.by,
  split.by = NULL,
  row.by = NULL,
  colors = NULL,
  reduction = "umap",
  pt.size = 0.1,
  col.split = NULL,
  row.split = NULL
)

PlotTrendAndUMAP(
  seurat_object,
  group.by,
  split.by = NULL,
  row.by = NULL,
  colors = NULL,
  reduction = "umap",
  pt.size = 0.1,
  bar.width = 0.6,
  label.size = 3,
  legendnrow = 2,
  legendtitle = NA,
  trend_width = 1,
  split.col.levels = NULL,
  split.cols = NULL,
  split.rows = NULL
)
}
\arguments{
\item{seurat_object}{A Seurat object.}

\item{group.by}{Character. Metadata column for point colour.}

\item{split.by}{Character. Metadata column for columns (samples/conditions).}

\item{row.by}{Character or \code{NULL}. Metadata column for rows.}

\item{show_trend}{Logical. Add a stacked composition trend panel to the
left. Default \code{FALSE}.}

\item{show_labels}{Logical. Overlay numbered cluster labels. Default
\code{FALSE}.}

\item{label_by}{Character or \code{NULL}. Column for label centroids.
\code{NULL} → \code{group.by}.}

\item{colors}{Named colour vector. \code{NULL} auto-resolves from
PrepObject or \code{Nour_pal}.}

\item{reduction}{Character. Reduction name (case-insensitive).}

\item{pt.size}{Numeric. Point size. Default \code{0.1}.}

\item{label.size}{Numeric. Label text size. Default \code{3}.}

\item{bar.width}{Numeric. Bar width in the trend plot. Default \code{0.6}.}

\item{legendnrow}{Integer. Legend rows. Default \code{2}.}

\item{legendtitle}{Character or \code{NA}. Legend title.}

\item{trend_width}{Numeric. Relative width of the trend panel vs. one UMAP
column. Default \code{1}.}

\item{split.col.levels}{Character vector. Explicit ordering for
\code{split.by} levels.}

\item{output_dir}{Character or \code{NULL}. Directory to save a PDF.}

\item{object_name}{Character. Output file-name prefix.}

\item{subset_name}{Character. Optional second prefix.}

\item{...}{Ignored (forward compatibility).}
}
\value{
A \code{ggpubr} combined plot.
}
\description{
Produces a UMAP grid where \strong{columns} correspond to \code{split.by}
levels and \strong{rows} (optionally) to \code{row.by} levels.  No empty
panels: samples missing from a row are omitted rather than shown as blank
bordered boxes.  The optional composition trend panel
(\code{show_trend = TRUE}) is placed to the left and stacked with the same
\code{ggarrange} row structure so row heights always align.
}
\section{Functions}{
\itemize{
\item \code{PlotGridUMAP()}: Deprecated — use \code{PlotDimPlots(show_trend=FALSE)}.

\item \code{PlotTrendAndUMAP()}: Deprecated — use \code{PlotDimPlots(show_trend=TRUE)}.

}}
