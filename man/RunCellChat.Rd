% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cellchat.R
\name{RunCellChat}
\alias{RunCellChat}
\title{Run the full CellChat analysis pipeline per group}
\usage{
RunCellChat(
  seurat_object = NULL,
  cell.by,
  group.by,
  groups = NULL,
  identity_column = NULL,
  group.col = NULL,
  species = "human",
  colors = NULL,
  output_dir,
  robj_dir = NULL,
  min.cells = 0L,
  thresh.p = 0.05,
  run.patterns = TRUE,
  pattern_k_range = 3:10,
  vertex.receiver = seq(1, 4),
  save.rds = TRUE,
  resume = FALSE,
  redo_plots = FALSE,
  assay_type = NULL
)
}
\arguments{
\item{seurat_object}{A Seurat object.}

\item{groups}{Character vector or \code{NULL}. Specific levels of \code{group.col} to
process. \code{NULL} uses all levels.}

\item{identity_column}{Character. Metadata column for cell identities (y-axis of
chord/circle diagrams, e.g., \code{"subAssignment"}).}

\item{group.col}{Character. Metadata column whose levels each get their own
CellChat run (e.g., \code{"Group"}).}

\item{species}{Character. Determines which CellChatDB ligand-receptor
database and PPI network are loaded. One of:
\itemize{
\item \code{"human"} (default) — uses \code{CellChatDB.human} and \code{PPI.human}
\item \code{"mouse"} — uses \code{CellChatDB.mouse} and \code{PPI.mouse}
}}

\item{colors}{Named character vector mapping \code{identity_column} levels to colours.
\code{NULL} uses CellChat's default palette. Tip: use the same colour vector as
your Seurat DimPlots so all figures are consistent.}

\item{output_dir}{Character. Root directory for all output. Per-group
sub-folders are created automatically.}

\item{robj_dir}{Character or \code{NULL}. Directory for RDS checkpoint files.
Defaults to \code{file.path(output_dir, "CellChatObjects")}.}

\item{min.cells}{Integer. Minimum number of cells a cell type must have in
a given condition for its interactions to be retained by
\code{\link[CellChat:filterCommunication]{CellChat::filterCommunication()}}. Increase to reduce noise from rare
populations. Default \code{0} (keep all).}

\item{thresh.p}{Numeric. P-value threshold used by
\code{\link[CellChat:identifyOverExpressedGenes]{CellChat::identifyOverExpressedGenes()}} and
\code{\link[CellChat:computeCommunProbPathway]{CellChat::computeCommunProbPathway()}}. Lowering this (e.g. \code{0.01}) makes
the analysis more conservative; raising it (e.g. \code{0.1}) recovers more
interactions. Default \code{0.05}.

\strong{Note:} this is the pipeline significance threshold. The \code{thresh}
parameter in \code{\link[=CompareCellChat]{CompareCellChat()}} independently controls which L-R
interactions are \emph{shown} in comparison plots.}

\item{run.patterns}{Logical. Run communication pattern analysis for
outgoing and incoming signals across k values in \code{pattern_k_range}?
Adds substantially to run time. Default \code{TRUE}.}

\item{pattern_k_range}{Integer vector. Range of latent pattern numbers (k)
tested by \code{\link[CellChat:identifyCommunicationPatterns]{CellChat::identifyCommunicationPatterns()}}. Each k×direction
combination produces one PDF. Default \code{3:10}. Reduce (e.g. \code{3:6}) if
run time is a concern.}

\item{vertex.receiver}{Integer vector. Indices of receiver cell types for
the hierarchy layout in per-pathway plots. Default \code{seq(1, 4)} (first
four cell types). Adjust to match your biological receiver populations.}

\item{save.rds}{Logical. Save each CellChat object as an RDS file?
Default \code{TRUE}.}

\item{resume}{Logical. Skip groups that have already completed and recover
groups interrupted mid-run. Default \code{FALSE}. Requires \code{save.rds = TRUE}
and \code{output_dir} to be set. Two checkpoints are used:
\itemize{
\item \strong{Pipeline checkpoint} (\verb{\{grp\}CellChat_ckpt.rds}): written
immediately after \code{netAnalysis_computeCentrality} (before any
plotting). If a run is interrupted during plotting or pattern
analysis, this file lets the next run skip the expensive pipeline
steps and jump straight to visualizations.
\item \strong{Final RDS} (\verb{\{grp\}CellChat.rds}): written at the end of a
successful group run and replaces the checkpoint. If this file
exists, that group is considered fully complete and skipped.
}}

\item{redo_plots}{Logical. Re-generate all per-group visualisation PDFs from
existing RDS files without re-running the communication probability
pipeline. Default \code{FALSE}. When \code{TRUE}:
\itemize{
\item For each group, \verb{\{grp\}CellChat.rds} is loaded from \code{robj_dir}.
\item All plotting code (circle, pathway, bubble, scatter, patterns) is
re-executed using the loaded object.
\item The pipeline (\code{computeCommunProb}, \code{aggregateNet}, etc.) is
completely skipped — the \code{seurat_object} argument is ignored.
\item Use this after \code{\link[=RenameCellTypeInCC]{RenameCellTypeInCC()}} + \code{saveRDS()} to regenerate
figures with corrected cell-type labels.
}}
}
\value{
A named list of CellChat objects (one per group level processed).
}
\description{
For each level of \code{group.col} in the Seurat object, subsets the data,
builds a CellChat object, runs the standard pipeline
(overexpressed genes → interactions → communication probability →
pathway probability → aggregate network → communication patterns),
and writes visualizations + the CellChat RDS file to \code{output_dir}.
}
