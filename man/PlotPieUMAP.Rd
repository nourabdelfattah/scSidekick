% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz_composition.R
\name{PlotPieUMAP}
\alias{PlotPieUMAP}
\title{UMAP with Pie Charts at Cluster Centroids}
\usage{
PlotPieUMAP(
  seurat_object,
  group.by,
  split.by,
  reduction = "umap",
  pie_scale = 1,
  bg_color = "lightgrey",
  bg_alpha = 0.3,
  pt.size = 0.4,
  split_colors = NULL,
  label = TRUE,
  label_size = 3,
  output_dir = NULL,
  object_name = "",
  subset_name = ""
)
}
\arguments{
\item{seurat_object}{A Seurat object.}

\item{group.by}{Metadata column defining the groups/clusters. One pie chart
is placed at each group's centroid in the reduction space.}

\item{split.by}{Metadata column whose level proportions define the pie
slices (e.g. \code{"Sample"} shows how much of each cluster is each
sample).}

\item{reduction}{Dimensionality reduction to use. Default \code{"umap"}.}

\item{pie_scale}{Scaling multiplier for pie radius (1 = auto-sized).
Increase for larger pies, decrease if pies overlap. Default \code{1}.}

\item{bg_color}{Background scatter point colour. Default \code{"lightgrey"}.}

\item{bg_alpha}{Background scatter point transparency. Default \code{0.3}.}

\item{pt.size}{Background scatter point size. Default \code{0.4}.}

\item{split_colors}{Named character vector of colours for \code{split.by}
levels. Auto-assigned from PrepObject or \code{Nour_pal} if \code{NULL}.}

\item{label}{Logical. Add cluster labels at centroids. Default \code{TRUE}.}

\item{label_size}{Font size for cluster labels. Default \code{3}.}

\item{output_dir}{Directory to save a PDF. \code{NULL} = no save.}

\item{object_name}{Label prefix for output file names.}

\item{subset_name}{Optional subset label.}
}
\value{
A \code{ggplot2} object.
}
\description{
Overlays proportional pie charts on a UMAP (or any 2D reduction) - one pie
per level of \code{group.by} - where each slice shows the fraction of cells
from each level of \code{split.by} (e.g. sample composition per cluster).

Uses \code{ggforce::geom_arc_bar} for stable pie rendering that works
correctly with ggplot2 >= 3.4 (unlike the old \code{coord_polar} approach).
}
