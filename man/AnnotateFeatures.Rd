% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotate_features.R
\name{AnnotateFeatures}
\alias{AnnotateFeatures}
\title{Annotate Seurat features with genomic metadata}
\usage{
AnnotateFeatures(
  seurat_object,
  species,
  assay = NULL,
  annotation_file = NULL,
  force = FALSE
)
}
\arguments{
\item{seurat_object}{A Seurat object.}

\item{species}{Character. One of \code{"mouse"}, \code{"human"}, \code{"zebrafish"}.}

\item{assay}{Character. Assay to annotate. Defaults to \code{DefaultAssay()}.}

\item{annotation_file}{Character or \code{NULL}. Path to a user-supplied TSV/CSV
(gene symbol as rowname; any recognized chromosome/start/end column names
are accepted). Bypasses all automatic sources when supplied.}

\item{force}{Logical. Re-annotate even if \code{nk_chr} column is already
present. Default \code{FALSE}.}
}
\value{
The Seurat object with new columns in its feature metadata:
\code{nk_chr}, \code{nk_start}, \code{nk_end}, \code{nk_strand}, \code{nk_biotype},
\code{nk_ensembl_id}, \code{nk_description}.
}
\description{
Adds chromosome location, gene biotype, Ensembl ID, and other genomic
metadata as columns to the feature-level metadata of a Seurat object.
Annotation is fetched from (in order): local cache → biomaRt →
AnnotationHub/EnsDb → precomputed download → user-supplied file.
Successful results are cached in \verb{~/.cache/R/scSidekick/} so the network
call is only made once per species.
}
\seealso{
\code{\link[=GetFeatures]{GetFeatures()}}, \code{\link[=GetGeneOrder]{GetGeneOrder()}}
}
