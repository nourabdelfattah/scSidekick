% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz_composition.R
\name{PlotAlluvial}
\alias{PlotAlluvial}
\title{Parallel Sets (Alluvial) Plot}
\usage{
PlotAlluvial(
  data,
  axes = NULL,
  group.by = NULL,
  split.by = NULL,
  donor.by = NULL,
  facet.by = NULL,
  ncol_facet = NULL,
  alpha = 0.75,
  axis_width = 0.1,
  label = TRUE,
  label_size = 4,
  label_nudge = 0.1,
  group_colors = NULL,
  split_colors = NULL,
  axes_colors = list(),
  axis.labels = NULL,
  facet.spacing = 1,
  horizontal = FALSE,
  label.inside = FALSE,
  title.size = 13,
  title.rotation = 0,
  output_dir = NULL,
  object_name = "",
  pdf_width = 7,
  pdf_height = 5.5
)
}
\arguments{
\item{data}{A Seurat object \strong{or} a plain \code{data.frame}/\code{tibble}
containing the metadata columns.}

\item{axes}{Character vector of \strong{2 or more} column names to use as axes,
from left to right (e.g. \code{c("BraakStage", "ThalPhase", "CERAD")}).
Stream color is determined by the first axis.}

\item{group.by}{Character. Left-axis column (retained for backwards
compatibility).  Ignored when \code{axes} is supplied.}

\item{split.by}{Character. Right-axis column (retained for backwards
compatibility).  Ignored when \code{axes} is supplied.}

\item{donor.by}{Character or \code{NULL}.  Metadata column identifying the
donor / patient (e.g. \code{"Donor.ID"}).  When supplied, metadata is
first deduplicated to one row per donor so stream widths reflect
\emph{donor counts} rather than cell counts - the right choice for
patient-level pathological scores (BraakStage, Thal phase, CERAD, etc.).
Default \code{NULL}.}

\item{facet.by}{Character or \code{NULL}.  Metadata column to facet by -
one alluvial panel per level (e.g. \code{"Diagnosis"}).  Default
\code{NULL}.}

\item{ncol_facet}{Integer or \code{NULL}.  Number of panel columns when
\code{facet.by} is used.  Auto-computed (max 3) if \code{NULL}.}

\item{alpha}{Numeric. Stream transparency. Default \code{0.75}.}

\item{axis_width}{Numeric. Width of the axis rectangles.  The stream width
is \code{1.5 × axis_width}.  Default \code{0.1}.}

\item{label}{Logical. Annotate each axis segment with its level name.
Default \code{TRUE}.}

\item{label_size}{Numeric. Label font size. Default \code{4}.}

\item{label_nudge}{Numeric. Horizontal gap between axis bar and label.
Default \code{0.1}.}

\item{group_colors}{Named character vector of colors for the first axis
(streams + left axis).  Auto-assigned if \code{NULL}.}

\item{split_colors}{Named character vector of colors for the last axis.
Auto-assigned if \code{NULL}.}

\item{axes_colors}{Named \code{list} of color vectors, keyed by column
name (e.g. \code{list(BraakStage = BraakColors, ThalPhase = ThalColors)}).
Takes precedence over \code{group_colors}/\code{split_colors}.}

\item{axis.labels}{Character vector of display labels for the axes, in the
same order as \code{axes}.  Use this to show short/readable names instead
of raw column names (e.g. \code{c("AD neuropath.", "Cognitive status",
  "Dementia")}).  \code{NULL} = use column names as-is.}

\item{facet.spacing}{Numeric (0-1+). Scale factor applied to the auto-computed
left and right panel margins when \code{facet.by} is used.  Values below
1 shrink the gap between panels (try \code{0.5}); values above 1 widen it.
Default \code{1} (no change).}

\item{horizontal}{Logical. If \code{TRUE}, rotate the diagram 90 degrees so
axis bars run horizontally using \code{coord_flip()}.  Best combined with
\code{label.inside = TRUE}.  Default \code{FALSE}.}

\item{label.inside}{Logical. If \code{TRUE}, place category labels centered
inside the colored bars in white text instead of outside.  Default
\code{FALSE}.}

\item{title.size}{Numeric. Font size for the axis name labels (e.g. "Sex",
"Group").  Default \code{13}.}

\item{title.rotation}{Numeric. Rotation angle (degrees) for the axis name
labels.  Default \code{0} (horizontal).}

\item{output_dir}{Directory to save a PDF.  \code{NULL} = no save.}

\item{object_name}{Label prefix for the output filename.}

\item{pdf_width, pdf_height}{PDF dimensions per panel in inches.  Default
\code{7 x 5.5}.}
}
\value{
A \code{ggplot2} or \code{patchwork} object (invisibly).
}
\description{
Creates a parallel sets (alluvial) chart - a clean alternative to SCP's
alluvials that preserves your own color palettes and theme.

Supports \strong{two or more axes} via the \code{axes} parameter.  Passing three
or more columns (e.g. \code{axes = c("BraakStage", "ThalPhase", "CERAD")})
produces a multi-axis diagram ideal for multiparametric comparisons.

Use \code{donor.by} to aggregate to the \strong{donor / patient level} before
computing counts - streams then show donor flows rather than cell flows.
This is the recommended approach for neuropathology scores, clinical
metadata, or any variable that is defined per-individual rather than
per-cell.

When \code{facet.by} is supplied, one alluvial panel is drawn per level and
assembled with \pkg{patchwork}.  Plot margins are auto-expanded based on the
longest axis label so text is never clipped.
}
