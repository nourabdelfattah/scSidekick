% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz_de.R
\name{RunEnrichment}
\alias{RunEnrichment}
\title{Run Over-Representation Analysis and Plot Results}
\usage{
RunEnrichment(
  features = NULL,
  deg_df = NULL,
  gene_column = NULL,
  fc_column = NULL,
  padj_column = NULL,
  group_column = NULL,
  fc_cutoff = 0.25,
  padj_cutoff = 0.05,
  direction = "up",
  databases = c("MSigDB_Hallmark_2020", "GO_Biological_Process_2023", "KEGG_2021_Human"),
  top_n_plot = 20,
  plot_type = "dot",
  padj_cutoff_plot = 0.05,
  output_dir = NULL,
  object_name = "",
  subset_name = ""
)
}
\arguments{
\item{features}{Character vector of gene symbols to test.  Provide either
this or \code{deg_df}, not both.}

\item{deg_df}{Data frame of DE results (presto or FindMarkers).  Genes are
filtered by \code{fc_cutoff}, \code{padj_cutoff}, and \code{direction}
before testing.}

\item{gene_column}{Gene name column in \code{deg_df}. Auto-detected if
\code{NULL}.}

\item{fc_column}{logFC column in \code{deg_df}. Auto-detected if \code{NULL}.}

\item{padj_column}{Adjusted p-value column. Auto-detected if \code{NULL}.}

\item{group_column}{Column in \code{deg_df} to loop over (e.g.
\code{"group"}). \code{NULL} = test all significant genes together.}

\item{fc_cutoff}{Minimum absolute logFC to include a gene. Default
\code{0.25}.}

\item{padj_cutoff}{Maximum adjusted p-value cutoff. Default \code{0.05}.}

\item{direction}{Which DEGs to test: \code{"up"} (logFC > 0), \code{"down"}
(logFC < 0), or \code{"both"}. Default \code{"up"}.}

\item{databases}{Character vector of enrichR database names to query.
See \code{enrichR::listEnrichrDbs()} for all options.  Default:
\code{c("MSigDB_Hallmark_2020", "GO_Biological_Process_2023", "KEGG_2021_Human")}.}

\item{top_n_plot}{Maximum number of terms to show per database. Default
\code{20}.}

\item{plot_type}{Type of plot passed to \code{\link{PlotEnrichment}}.
Default \code{"dot"}.}

\item{padj_cutoff_plot}{Significance cutoff for filtering enrichment results
before plotting. Default \code{0.05}.}

\item{output_dir}{Directory to save PDFs and result CSVs.  \code{NULL} =
no save, print only.}

\item{object_name}{Label prefix for output file names.}

\item{subset_name}{Optional subset label.}
}
\value{
A nested list: \code{[[group]][[database]]} containing enrichR
result data frames.  Invisibly when \code{output_dir} is set.
}
\description{
Runs over-representation analysis (ORA) using \pkg{enrichR} on a gene list
or on significant DEGs from a data frame, then automatically calls
\code{\link{PlotEnrichment}} to visualise the results.  When
\code{group_column} is supplied, analysis is run separately per group.
}
\seealso{
\code{\link{PlotEnrichment}}, \code{\link{PlotVolcano}}
}
