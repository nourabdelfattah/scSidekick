% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pptx.R
\name{create_analysis_pptx}
\alias{create_analysis_pptx}
\title{Build a PowerPoint summary from an analysis output folder}
\usage{
create_analysis_pptx(
  output_dir,
  params_json = NULL,
  out_pptx = NULL,
  slide_format = "standard",
  slide_width = NULL,
  slide_height = NULL,
  pdf_density = 150,
  include_legends = TRUE,
  legend_font_size = 8,
  include = NULL,
  exclude = NULL
)
}
\arguments{
\item{output_dir}{Character. Folder containing PDF figures.}

\item{params_json}{Character or \code{NULL}. Path to \code{analysis_params.json}.
When \code{NULL} (default) the function walks up the directory tree from
\code{output_dir} until it finds the file - this lets you call
\code{create_analysis_pptx} on a sub-folder (e.g., the CellChat or GSEA
output directory) and still get the shared analysis parameters from the
parent folder.}

\item{out_pptx}{Character or \code{NULL}.  Where to save the \code{.pptx}.  When
\code{NULL} (default) a filename is auto-generated from the dataset/subset
labels and the current date, saved inside \code{output_dir}.}

\item{slide_format}{Character. Slide aspect ratio preset: \code{"standard"}
(4:3, 10 × 7.5 in, default) or \code{"widescreen"} (16:9, 13.33 × 7.5 in).
Override with explicit \code{slide_width} / \code{slide_height} if needed.}

\item{slide_width, slide_height}{Numeric. Explicit slide dimensions in
inches. When provided these override \code{slide_format}.}

\item{pdf_density}{Integer. DPI used when rasterising PDFs.  Increase
(e.g. \code{200}) for sharper figures at the cost of speed. Default \code{150}.}

\item{include}{Character vector or \code{NULL}. Section names to include; all
other sections are dropped. When \code{NULL} (default) all sections are
included. PDFs are assigned to sections by filename pattern matching -
see \code{.pptx_section_rules} for the rules. Valid section names:
\itemize{
\item \code{"Quality Control"} - QC density plots, doublet scores, filtration
barplots
\item \code{"Dimensionality Reduction"} - PCA elbow, UMAP before/after
Harmony, RNA UMAPs
\item \code{"Cluster Markers"} - ComplexHeatmap heatmaps, presto marker
tables
\item \code{"Dotplots"} - any PDF with "dotplot" in the filename
\item \code{"Cell Type Annotation"} - AutoAssignment UMAPs, SingleR, scType,
featureheatmaps, assignment helper
\item \code{"All UMAPs"} - the combined multi-panel UMAP PDF
\item \code{"Composition"} - barplots, chord diagrams, rose/trend plots,
iteration-with-bar figures
\item \code{"Feature Maps"} - GenerateFeatureMaps per-gene UMAPs
\item \code{"Pathway Analysis"} - GSEA heatmaps and lollipop PDFs
\item \code{"CellChat"} - RunCellChat, CompareCellChat, and
RankCellChatPathways output PDFs
\item \code{"Other"} - anything not matched by the rules above
}}

\item{exclude}{Character vector or \code{NULL}. Section names to drop (applied
after \code{include}). \code{NULL} excludes nothing. Same valid values as \code{include}.
Typical use: \code{exclude = c("Feature Maps", "CellChat")} to keep the main
analysis compact while running separate sub-folder PPTXs for those
heavy sections.}
}
\value{
Invisibly returns the path to the saved \code{.pptx} file.
}
\description{
Collects all PDFs in \code{output_dir}, converts them to images with
\code{\link[magick:editing]{magick::image_read_pdf()}}, groups them into labelled sections based on
filename patterns, and assembles a polished \code{.pptx} via the
\link[officer:officer]{officer} package.
}
\details{
A parameters / methods overview slide is generated automatically from
\code{analysis_params.json} (written by \code{\link[=log_analysis_params]{log_analysis_params()}}).
}
