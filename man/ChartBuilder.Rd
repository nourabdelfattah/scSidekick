% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chart_builder.R
\name{ChartBuilder}
\alias{ChartBuilder}
\title{Open the Chart Builder app pre-loaded with an R data frame}
\usage{
ChartBuilder(
  data,
  app_dir = getOption("nourkit.chart_builder_dir", system.file("Chart_Builder", package =
    "scSidekick")),
  filename = NULL,
  max_rows = NULL,
  launch = TRUE
)
}
\arguments{
\item{data}{A \code{data.frame} (or object coercible with \code{\link[=as.data.frame]{as.data.frame()}}),
or a Seurat object - in which case its \code{meta.data} slot is used.}

\item{app_dir}{Character. Path to the Chart Builder app directory (the folder
containing \code{chart_builder.html}). Defaults to the bundled copy inside the
scSidekick package (\code{inst/Chart_Builder}), or
\code{getOption("nourkit.chart_builder_dir")} if set.}

\item{filename}{Character. Name shown in the app for the loaded dataset.
Defaults to the deparsed name of \code{data} with a \code{.csv} suffix.}

\item{max_rows}{Integer or \code{NULL}. If set and \code{data} has more rows, a random
sample of \code{max_rows} rows is sent (keeps the payload small). \code{NULL}
(default) sends everything; the app itself samples large data for
plotting. A warning is issued for very large frames.}

\item{launch}{Logical. Open the browser? Default \code{TRUE}. \code{FALSE} builds the
session directory and returns its path without opening (useful for tests).}
}
\value{
(Invisibly) the path to the temporary session directory containing
the launched copy of the app.
}
\description{
Launches the standalone Chart Builder (an HTML/CSS/JS Plotly app) in the
default browser with \code{data} already loaded, dropping the user straight onto
the data-setup screen. This is a one-way, interactive launcher: data flows
from R into the browser, and the finished chart is exported from within the
app. There is no automated round-trip back to R.
}
\details{
The app is copied into a fresh temporary directory on each call, so the
source app is never modified and concurrent sessions do not collide. The app
loads its charting libraries (Plotly, PapaParse, SheetJS) from CDNs, so an
internet connection is required for it to render.
}
