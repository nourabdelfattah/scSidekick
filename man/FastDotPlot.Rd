% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dotplots.R
\name{FastDotPlot}
\alias{FastDotPlot}
\title{Fast dot plot with regex gene selection and k-group gene clustering}
\usage{
FastDotPlot(
  seurat_object,
  assay = "RNA",
  features = NULL,
  pattern = NULL,
  group.by = "seurat_clusters",
  split.by = NULL,
  ClusterGroups = FALSE,
  ClusterGenes = TRUE,
  k_genes = 1,
  min.pct.exp = 15,
  col.min = 0,
  col.max = 2.5,
  scale.min = 5,
  scale.max = 100,
  dot.scale = 6,
  output_dir = NULL,
  file_name = NULL
)
}
\arguments{
\item{seurat_object}{A Seurat object.}

\item{assay}{Character. Assay to use. Default \code{"RNA"}.}

\item{features}{Character vector of gene names, or \code{NULL} if using
\code{pattern}.}

\item{pattern}{Character regex. All matching genes in \code{rownames(seurat_object)} are
used. Ignored if \code{features} is provided.}

\item{group.by}{Character. Metadata column for y-axis. Default
\code{"GlobalAssignment"}.}

\item{split.by}{Character or \code{NULL}. Metadata column for row facets.}

\item{ClusterGroups}{Logical. Cluster y-axis groups? Default \code{FALSE}.}

\item{ClusterGenes}{Logical. Cluster genes on x-axis? Default \code{TRUE}.}

\item{k_genes}{Integer. Number of gene clusters (panels) to cut the
dendrogram into. \code{1} = no cutting (single panel). Default \code{1}.}

\item{min.pct.exp}{Numeric. Minimum percent expression threshold.
Default \code{15}.}

\item{col.min, col.max}{Numeric. Color clamp limits. Defaults \code{0}, \code{2.5}.}

\item{scale.min, scale.max}{Numeric. Size clamp limits. Defaults \code{5}, \code{100}.}

\item{dot.scale}{Numeric. Max dot radius. Default \code{6}.}

\item{output_dir}{Character or \code{NULL}. Directory to save a PDF.
\code{NULL} (default) returns the plot without saving.  When
\code{NULL}, the function walks up to the \code{output_dir} stored by
\code{\link{PrepObject}}, unless \code{AutoSavePlots = FALSE} was set
there.}

\item{file_name}{Character or \code{NULL}. Base name (no extension) for the
saved PDF. \code{NULL} (default) auto-deduces a name that always includes
\code{group.by} and adds the \code{pattern} when one is used, e.g.
\code{object_name_group.by_pattern_<pattern>_FastDotPlot}.}
}
\value{
A ggplot2 object (invisibly when saved to disk).
}
\description{
Like \code{\link[=SplitDotPlot2]{SplitDotPlot2()}} but accepts genes via a character vector \strong{or} a
regex pattern, and can slice the gene dendrogram into \code{k_genes} labelled
panels using \code{cutree()}.
}
