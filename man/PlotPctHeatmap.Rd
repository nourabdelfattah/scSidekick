% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter_features.R
\name{PlotPctHeatmap}
\alias{PlotPctHeatmap}
\title{Heatmap of per-group gene expression percentages}
\usage{
PlotPctHeatmap(
  pct_table,
  title = "\% cells expressing each gene",
  output_dir = NULL,
  file_name = NULL
)
}
\arguments{
\item{pct_table}{Data frame as returned by \code{\link[=FilterGenesByPct]{FilterGenesByPct()}} (gene names
as rownames or in a \code{gene} column; remaining columns are group names).}

\item{title}{Character. Plot title. Default \code{"\% cells expressing each gene"}.}

\item{output_dir}{Character or \code{NULL}. Directory to save a PDF. \code{NULL}
returns the plot.}

\item{file_name}{Character or \code{NULL}. Base name (no extension) for the
saved PDF. Defaults to \code{"PctHeatmap"}.}
}
\value{
A ggplot2 object (invisibly when saved).
}
\description{
Visualizes the \code{pct_table} returned by \code{\link[=FilterGenesByPct]{FilterGenesByPct()}} as a tile
heatmap — genes on the y-axis, groups on the x-axis, color encoding the
percent of cells expressing each gene.
}
\seealso{
\code{\link[=FilterGenesByPct]{FilterGenesByPct()}}
}
