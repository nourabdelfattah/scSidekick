% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trend_plots.R
\name{PlotTrendLabeled}
\alias{PlotTrendLabeled}
\title{Labeled stacked composition trend plot}
\usage{
PlotTrendLabeled(
  seurat_object,
  stat.by,
  group.by,
  split.by,
  colors = NULL,
  group.levels = NULL,
  label.side = "right",
  bar.width = 0.6,
  label.size = 3,
  label.nudge = 0.1,
  label.margin = 120,
  segment.size = 0.4,
  max.overlaps = 100,
  hjust = 1,
  show.legend = FALSE,
  border.size = 0.5,
  base_size = 12,
  nrow = NULL,
  ncol = NULL,
  output_dir = NULL,
  file_name = NULL,
  object_name = NULL,
  subset_name = NULL,
  width = NULL,
  height = NULL
)
}
\arguments{
\item{seurat_object}{A Seurat object.}

\item{stat.by}{Character. Metadata column whose proportions are plotted
(e.g., cell type assignments). This becomes the fill color.}

\item{group.by}{Character. Ordered x-axis variable (e.g., \code{"TimePoint"}).}

\item{split.by}{Character. Metadata column that defines independent panels.}

\item{colors}{Named character vector mapping \code{stat.by} levels to colors.}

\item{group.levels}{Character vector. Explicit ordering of \code{group.by}
levels. \code{NULL} uses factor or alphabetical order.}

\item{label.side}{Character. Where to place labels: \code{"right"} (default,
last x position) or \code{"both"} (first and last).}

\item{bar.width}{Numeric. Width of stacked bars. Default \code{0.6}.}

\item{label.size}{Numeric. ggrepel text size. Default \code{3.5}.}

\item{label.nudge}{Numeric. Horizontal nudge for labels (in x-axis units).
Default \code{0.3}.}

\item{label.margin}{Numeric. Right plot margin (pt) to accommodate labels.
Default \code{80}.}

\item{segment.size}{Numeric. Connector line width. Default \code{0.4}.}

\item{max.overlaps}{Integer. Passed to \code{\link[ggrepel:geom_text_repel]{ggrepel::geom_text_repel()}}.
Default \code{100}.}

\item{show.legend}{Logical. Show a fill legend? Default \code{FALSE} (labels
replace the legend).}

\item{border.size}{Numeric. Panel border line width. Default \code{0.5}.}

\item{base_size}{Numeric. Base font size. Default \code{12}.}

\item{nrow, ncol}{Integer or \code{NULL}. Number of rows/columns for the panel
grid. \code{NULL} (default) auto-arranges into a single row.}

\item{output_dir}{Character or \code{NULL}. Directory to save the PDF. Walks up
from \code{PrepObject()} settings when \code{NULL}.}

\item{file_name}{Character or \code{NULL}. Base filename (no extension). Auto-
deduced from \code{object_name}, \code{subset_name}, and column names when \code{NULL}.}

\item{object_name}{Character. Prefix for the auto-deduced filename.}

\item{subset_name}{Character. Second prefix component.}

\item{width, height}{Numeric. PDF dimensions in inches. Auto-sized when \code{NULL}.}
}
\value{
A patchwork object (one panel per \code{split.by} level). Returns
invisibly when saved to disk.
}
\description{
Computes cell-type proportions across ordered levels of a grouping variable
(e.g., time points or conditions), displays them as a stacked area + bar
chart, and places \code{\link[ggrepel:geom_text_repel]{ggrepel::geom_text_repel()}} labels at the rightmost
position so each cell type is identified without a legend.
}
\details{
Panels are faceted by \code{split.by} and combined with
\code{\link[patchwork:wrap_plots]{patchwork::wrap_plots()}}. One panel per \code{split.by} level is produced.
}
