% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coexpression.R
\name{CorrelateGene}
\alias{CorrelateGene}
\title{Correlate one gene against all others within each cell type}
\usage{
CorrelateGene(
  seurat_object,
  gene,
  group_by,
  level = c("sc", "pseudobulk", "group"),
  compare_by = NULL,
  sample_column = NULL,
  group_column = NULL,
  assay = NULL,
  layer = "data",
  method = c("spearman", "pearson"),
  expressed_only = TRUE,
  min_pct = 10,
  min_cells = 20L,
  min_donors = 5L,
  min_groups = 3L
)
}
\arguments{
\item{seurat_object}{A Seurat object.}

\item{gene}{Character. The gene of interest (GOI).}

\item{group_by}{Character. Cell-type identity column.}

\item{level}{Character. \code{"sc"} (default), \code{"pseudobulk"}, or \code{"group"}.}

\item{compare_by}{Character or \code{NULL}. Optional metadata column whose levels
define independent comparison groups (e.g. \code{"Sex"}, \code{"Diagnosis"}).
When supplied, correlation is computed separately within each level so you
can ask which genes are correlated with GOI in group A but not group B.}

\item{sample_column}{Character. Donor/sample ID. Required for \code{"pseudobulk"}.}

\item{group_column}{Character. Condition column. Required for \code{"group"}.}

\item{assay}{Character or \code{NULL}. Assay to use.}

\item{layer}{Character. Layer/slot for SC and group levels. Default \code{"data"}.}

\item{method}{Character. \code{"spearman"} (default) or \code{"pearson"}.}

\item{expressed_only}{Logical. SC level: restrict to GOI > 0 cells? Default
\code{TRUE}.}

\item{min_pct}{Numeric. SC level: minimum \% of (GOI-expressing) cells
that must express a tested gene. Default \code{10}.}

\item{min_cells}{Integer. SC level: minimum cells per cell type. Default
\code{20}.}

\item{min_donors}{Integer. Pseudobulk level: minimum donors. Default \code{5}.}

\item{min_groups}{Integer. Group level: minimum groups. Default \code{3}.}
}
\value{
A data frame: \code{gene}, \code{cell_type}, \code{r}, \code{p}, \code{padj}, \code{n}, \code{goi},
\code{level}, \code{method}, and (when \code{compare_by} is set) \code{compare_group}.
Sorted by cell type then descending \verb{|r|}.
}
\description{
Computes Spearman (or Pearson) correlation between a gene of interest (GOI)
and every other gene, separately within each cell type. Three resolution
levels are supported:
}
\details{
\itemize{
\item \strong{\code{"sc"}}: cell level. Restricts to cells where GOI > 0
(\code{expressed_only = TRUE}) by default to avoid co-absence artifacts from
dropout. Exploratory; validate hits at pseudobulk level.
\item \strong{\code{"pseudobulk"}}: donor level. Aggregates raw counts per donor x
cell type, converts to edgeR log-CPM, then runs Spearman across donors.
Statistically most valid.
\item \strong{\code{"group"}}: averages log-normalized expression within each
\verb{group_column x cell_type} combination, then correlates across groups.
}

When \code{compare_by} is supplied, the analysis is run separately within each
level of that variable (e.g. \code{compare_by = "Sex"} produces independent
correlation tables for males and females). Results gain a \code{compare_group}
column and \code{\link[=PlotCorrelation]{PlotCorrelation()}} will lay the groups out side by side.
}
\seealso{
\code{\link[=SplitByGene]{SplitByGene()}}, \code{\link[=PlotCorrelation]{PlotCorrelation()}}
}
