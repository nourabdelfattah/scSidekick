% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cnv.R
\name{RunInferCNV}
\alias{RunInferCNV}
\title{Run inferCNV copy-number inference}
\usage{
RunInferCNV(
  seurat_object,
  cell_type_col = "Assignment",
  ref_group_names,
  output_dir = NULL,
  species = NULL,
  chromosomes = NULL,
  gene_biotype = "protein_coding",
  annotation_file = NULL,
  downsample = 200L,
  cutoff = 0.1,
  HMM = FALSE,
  denoise = TRUE,
  scale_data = TRUE,
  cluster_by_groups = TRUE,
  num_threads = 4L,
  cnv_score_threshold = "auto",
  cnv_k = 2,
  resume = FALSE,
  caffeinate = FALSE
)
}
\arguments{
\item{seurat_object}{A Seurat object.}

\item{cell_type_col}{Character. Metadata column containing cell-type labels
used to split cells and define reference groups. Default \code{"Assignment"}.}

\item{ref_group_names}{Character vector. Cell-type labels treated as the
normal (diploid) reference — typically immune or stromal populations.}

\item{output_dir}{Character or \code{NULL}. Directory for inferCNV output and
saved figures. Walks up from \code{\link[=PrepObject]{PrepObject()}} settings when \code{NULL}.}

\item{species}{Character. \code{"mouse"}, \code{"human"}, or \code{"zebrafish"}. Used to
auto-generate gene order via \code{\link[=AnnotateFeatures]{AnnotateFeatures()}} if not yet annotated.}

\item{chromosomes}{Character or numeric vector of chromosomes to analyze.
\code{NULL} uses species-appropriate autosomes (mouse 1–19, human 1–22,
zebrafish 1–25).}

\item{gene_biotype}{Character. Biotype filter for the gene order table.
Default \code{"protein_coding"}.}

\item{annotation_file}{Character or \code{NULL}. Passed to \code{\link[=AnnotateFeatures]{AnnotateFeatures()}}
when manual gene annotation is preferred over the automatic sources.}

\item{downsample}{Integer. Maximum cells per identity class. \code{NULL} uses all
cells (not recommended for large objects). Default \code{200L}.}

\item{cutoff}{Numeric. inferCNV expression cutoff. Use \code{0.1} for 10x
Genomics and \code{1} for Smart-seq2. Default \code{0.1}.}

\item{HMM}{Logical. Run inferCNV's HMM for subclonal CNV calls? Dramatically
increases runtime. Default \code{FALSE}.}

\item{denoise}{Logical. Apply inferCNV denoising. Default \code{TRUE}.}

\item{scale_data}{Logical. Scale expression data before inference.
Default \code{TRUE}.}

\item{cluster_by_groups}{Logical. Cluster cells within their annotation
groups. Default \code{TRUE}.}

\item{num_threads}{Integer. Threads for inferCNV. Default \code{4L}.}

\item{cnv_score_threshold}{Numeric or \code{"auto"}. When \code{"auto"} (default),
the threshold is set to \verb{mean(ref_scores) + cnv_k × sd(ref_scores)} using
the reference cells' CNV score distribution — this is the data-driven
approach from Tirosh et al. 2016. Pass a numeric value to override.}

\item{cnv_k}{Numeric. Number of standard deviations above the reference mean
used for the auto threshold. Higher values = more conservative (fewer
malignant calls). Default \code{2}.}

\item{resume}{Logical. If \code{TRUE} and output files already exist, skip
re-running inferCNV and go straight to post-processing. Default \code{FALSE}.}

\item{caffeinate}{Logical. Prevent the machine from sleeping during the run.
Default \code{FALSE}.}
}
\value{
The input Seurat object with additional metadata columns.
}
\description{
Wraps the full inferCNV workflow: gene-order table generation (via
\code{\link[=AnnotateFeatures]{AnnotateFeatures()}} / \code{\link[=GetGeneOrder]{GetGeneOrder()}}), downsampling, object creation,
inference, post-processing of \code{map_metadata_from_infercnv.txt}, and
standard figure output. Returns the Seurat object with new metadata columns:
\code{CNV_score}, \code{CNV_Prediction}, \code{infercnv_subcluster}, and one
\code{proportion_cnv_chrN} column per chromosome.
}
\seealso{
\code{\link[=RunCopyKAT]{RunCopyKAT()}}, \code{\link[=AnnotateFeatures]{AnnotateFeatures()}}, \code{\link[=GetGeneOrder]{GetGeneOrder()}}
}
