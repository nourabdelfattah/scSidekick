% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/session_summary.R
\name{summarize_r_session}
\alias{summarize_r_session}
\title{Summarize an R session log}
\usage{
summarize_r_session(
  log_file = NULL,
  rhistory = NULL,
  output_dir = NULL,
  open = TRUE
)
}
\arguments{
\item{log_file}{Character or \code{NULL}.  Path to a \code{.log} file written by
\code{\link[=start_session_logger]{start_session_logger()}}.  When \code{NULL} (default) the most recent log in
\verb{~/.R/session_logs/} is used.}

\item{rhistory}{Character or \code{NULL}.  Path to an \code{.Rhistory} file to
summarize instead of a structured log.  Takes precedence over \code{log_file}.}

\item{output_dir}{Character or \code{NULL}.  Directory for the output files.
Defaults to the directory containing \code{log_file} (or \code{rhistory}), or the
current working directory.}

\item{open}{Logical.  If \code{TRUE} (default) and the session is interactive,
opens the markdown file in the editor after writing.}
}
\value{
Invisibly returns a named list with \verb{$md}, \verb{$r}, and \verb{$data}.
}
\description{
Parses a structured session log created by \code{\link[=start_session_logger]{start_session_logger()}} (or a
plain \code{.Rhistory} file) and writes two files into \code{output_dir}:
}
\details{
\itemize{
\item \code{session_YYYYMMDD_summary.md} - a markdown report with phase-by-phase
summaries, error counts, and a plot inventory.
\item \code{session_YYYYMMDD_clean.R}   - a deduplicated, phase-annotated R script
containing only the successful commands.
}
}
