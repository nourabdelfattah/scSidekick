% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pathway_leadingedge.R
\name{StackedVlnPlot}
\alias{StackedVlnPlot}
\title{Stacked Violin Plot for Multiple Features}
\usage{
StackedVlnPlot(
  seurat_object,
  features,
  pt.size = 0,
  plot.margin = grid::unit(c(-0.75, 0, -0.75, 0), "cm"),
  ...
)
}
\arguments{
\item{seurat_object}{A Seurat object.}

\item{features}{Character vector of features (genes) to plot. One panel per
feature.}

\item{pt.size}{Jitter point size (default \code{0} — no points).}

\item{plot.margin}{Plot margin around each panel as a
\code{grid::unit} object (default \code{unit(c(-0.75, 0, -0.75, 0), "cm")}).
Negative top/bottom values collapse space between stacked panels.}

\item{...}{Additional arguments forwarded to \code{Seurat::VlnPlot} (e.g.
\code{group.by}, \code{split.by}, \code{cols}).}
}
\value{
A \code{patchwork} object (one panel per feature, stacked in a
single column).
}
\description{
Builds a stacked vertical layout of per-feature violin plots using
\code{Seurat::VlnPlot}, assembled with \code{patchwork}. X-axis labels and
ticks are hidden on all panels except the bottom one; the gene name is shown
as the y-axis title (rotated 0\eqn{^\circ}) on each panel.
}
