% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pseudobulk.R
\name{PlotPseudoBulk}
\alias{PlotPseudoBulk}
\title{Pseudobulk box + dot plots for gene expression}
\usage{
PlotPseudoBulk(
  seurat_object,
  features,
  group.by,
  donor.by,
  split.by = NULL,
  row.by = NULL,
  exclude = NULL,
  min_cells = 0L,
  show_n = TRUE,
  add_stats = FALSE,
  comparisons = NULL,
  ref_group = NULL,
  hide_ns = FALSE,
  stat_alpha = 0.05,
  label_format = c("stars", "p.format"),
  point_size = 1.5,
  jitter_width = 0.15,
  box_width = 0.5,
  alpha = 0.7,
  ncol = NULL,
  colors = NULL,
  assay = "RNA",
  layer = "data",
  output_dir = NULL,
  object_name = "",
  pdf_width = NULL,
  pdf_height = NULL
)
}
\arguments{
\item{seurat_object}{A Seurat object.}

\item{features}{Character vector.  Gene names to plot.}

\item{group.by}{Character.  Categorical metadata column shown on the x-axis
(e.g. \code{"Cognitive.Status"} or \code{"Condition"}).}

\item{donor.by}{Character.  Metadata column that identifies one biological
replicate (e.g. \code{"Donor.ID"}).  One dot per unique value.}

\item{split.by}{Character or \code{NULL}.  Column facets within each panel
(e.g. \code{"CellType"}).}

\item{row.by}{Character or \code{NULL}.  Row facets within each panel.
Combined with \code{split.by} gives a two-way grid.}

\item{exclude}{Named list of values to drop before plotting.  Each name is
a column and each value is a character vector of levels to remove.
Example: \code{list(Cognitive.Status = "Reference")}.}

\item{min_cells}{Integer.  Drop pseudobulk groups (donor x group
combinations) with fewer than this many cells.  Default \code{0L}.}

\item{show_n}{Logical.  Annotate each x-axis group with the number of
donors (or pseudobulk samples) in that group after all filtering.
Shown as \code{n=X} below the x-axis ticks.  Default \code{TRUE}.}

\item{add_stats}{Logical.  Add Wilcoxon significance brackets via
\pkg{ggsignif}.  Default \code{FALSE}.}

\item{comparisons}{List of length-2 character vectors specifying pairs.
\code{NULL} (default) tests all pairs, or all vs \code{ref_group}.}

\item{ref_group}{Character or \code{NULL}.  Reference group for comparisons.}

\item{hide_ns}{Logical.  Suppress \code{"ns"} brackets.  Default
\code{FALSE}.}

\item{label_format}{One of \code{"stars"} (default) or \code{"p.format"}.}

\item{point_size}{Numeric.  Size of donor dots.  Default \code{1.5}.}

\item{jitter_width}{Numeric.  Jitter width for donor dots.  Default
\code{0.15}.}

\item{box_width}{Numeric.  Width of the box geom.  Default \code{0.5}.}

\item{alpha}{Numeric.  Fill transparency of boxes.  Default \code{0.7}.}

\item{ncol}{Integer or \code{NULL}.  Columns in the patchwork assembly.
\code{NULL} = \code{min(n_features, 3)}.}

\item{colors}{Named character vector of fill colors for \code{group.by}
levels.  \code{NULL} auto-resolves from \code{PrepObject} or
\code{SelectColors()}.}

\item{assay}{Character.  Seurat assay.  Default \code{"RNA"}.}

\item{layer}{Character.  Assay layer.  Default \code{"data"}.}

\item{output_dir}{Character or \code{NULL}.  Save directory.  Walks up from
\code{PrepObject}.}

\item{object_name}{Character.  Filename prefix.  Walks up from
\code{PrepObject}.}

\item{pdf_width}{Numeric or \code{NULL}.  Override auto-calculated PDF width.}

\item{pdf_height}{Numeric or \code{NULL}.  Override auto-calculated PDF height.}
}
\value{
A \code{ggplot2} object (single feature) or a \code{patchwork}
combined plot (multiple features).  Saved as PDF when \code{output_dir}
is available.
}
\description{
Plots mean gene expression per donor (or per donor x cell type, etc.) as
box plots with individual donor dots overlaid - the standard pseudobulk
visualization that avoids pseudo-replication.
}
\details{
Each dot represents one unique \code{donor.by} value (e.g. one patient).
Boxes show median, IQR, and 1.5x IQR whiskers.  Dots are black.

On first call, pseudobulk means are computed on the fly and a hint is
printed suggesting \code{\link{ComputePseudobulk}} for BPCells / large
datasets.  Pre-warm the cache with \code{ComputePseudobulk()} to make
subsequent plots instant.
}
\examples{
\dontrun{
# Pre-warm cache (recommended for BPCells / large datasets)
SeuratObj <- ComputePseudobulk(SeuratObj,
  group.by = c("Donor.ID", "Cognitive.Status"))

# Per-donor boxplot, exclude reference group, add stats
PlotPseudoBulk(SeuratObj,
  features  = c("STAT3", "APOE"),
  group.by  = "Cognitive.Status",
  donor.by  = "Donor.ID",
  exclude   = list(Cognitive.Status = "Reference"),
  add_stats = TRUE)

# Per-donor x per-cell-type (split into column facets)
SeuratObj <- ComputePseudobulk(SeuratObj,
  group.by = c("Donor.ID", "Cognitive.Status", "CellType"))

PlotPseudoBulk(SeuratObj,
  features   = "APOE",
  group.by   = "Cognitive.Status",
  donor.by   = "Donor.ID",
  split.by   = "CellType",
  min_cells  = 10,
  add_stats  = TRUE,
  ref_group  = "Control")
}
}
