% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coexpression.R
\name{SplitByGene}
\alias{SplitByGene}
\title{Split cells into expression groups per cell type}
\usage{
SplitByGene(
  seurat_object,
  gene,
  group_by,
  split = c("zero_vs_expressed", "quantile", "expressed_quantile"),
  quantile_range = c(0.25, 0.75),
  assay = NULL,
  layer = "data",
  col_name = NULL
)
}
\arguments{
\item{seurat_object}{A Seurat object.}

\item{gene}{Character. Gene to split on.}

\item{group_by}{Character. Cell-type identity column.}

\item{split}{Character. \code{"zero_vs_expressed"} (default), \code{"quantile"}, or
\code{"expressed_quantile"}.}

\item{quantile_range}{Numeric length-2. Quantile bounds for the two
threshold modes. Default \code{c(0.25, 0.75)}.}

\item{assay}{Character or \code{NULL}. Assay to use.}

\item{layer}{Character. Layer/slot. Default \code{"data"}.}

\item{col_name}{Character or \code{NULL}. New metadata column name. Defaults to
\code{"<gene>_split"}.}
}
\value{
Seurat object with a new metadata column. Prints a count table.
}
\description{
Adds a metadata column labeling each cell as \code{"high"} / \code{"low"} (or
\code{"expressed"} / \code{"zero"}) for a given gene. The split is computed
independently within each cell type. The column can be passed directly as
\code{group_column} in \code{\link[=RunGSEA_pseudobulk]{RunGSEA_pseudobulk()}} or used with \code{Seurat::FindMarkers}.
}
\details{
\strong{Split modes}
\describe{
\item{\code{"zero_vs_expressed"}}{GOI = 0 → \code{"zero"}; GOI > 0 →
\code{"expressed"}. Cleanest for pseudobulk: maps to donor-level proportion.}
\item{\code{"quantile"}}{Bottom \code{quantile_range[1]} → \code{"low"}; top
\code{1 - quantile_range[2]} → \code{"high"}; middle → \code{NA} (excluded).}
\item{\code{"expressed_quantile"}}{Same as \code{"quantile"} but among GOI > 0 cells
only. Zeros → \code{NA}. Avoids dropout in the low group but can confound
with library size - regress out \code{nCount_RNA} downstream.}
}
}
\seealso{
\code{\link[=CorrelateGene]{CorrelateGene()}}, \code{\link[=RunGSEA_pseudobulk]{RunGSEA_pseudobulk()}}
}
