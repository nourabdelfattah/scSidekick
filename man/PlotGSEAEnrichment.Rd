% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz_de.R
\name{PlotGSEAEnrichment}
\alias{PlotGSEAEnrichment}
\title{GSEA Running Score Mountain Plots - Direct or from RunGSEA Output Directory}
\usage{
PlotGSEAEnrichment(
  fgsea_result = NULL,
  ranked_stats = NULL,
  gsea_dir = NULL,
  database = NULL,
  search_terms = NULL,
  pathways = NULL,
  top_n = 5,
  label_levels = NULL,
  group_levels = NULL,
  split_levels = NULL,
  max_cols = 6,
  colors = c(pos = "#B40426", neg = "#3B4CC0"),
  ncol = 1,
  output_dir = NULL,
  object_name = "",
  subset_name = ""
)
}
\arguments{
\item{fgsea_result}{\emph{Direct mode only.} Data frame with fgsea output or
a path to a single RunGSEA CSV.  Must have \code{pathway}, \code{NES},
\code{leadingEdge}, and \code{size} columns.}

\item{ranked_stats}{\emph{Direct mode only.} Named numeric vector (gene →
AUC or logFC), sorted descending.}

\item{gsea_dir}{\emph{RunGSEA directory mode.} Path to the \code{output_dir}
used in \code{\link{RunGSEA}}.}

\item{database}{Database subfolder name (e.g. \code{"Hallmark"},
\code{"C2"}). \code{NULL} uses the first database found.}

\item{search_terms}{Keyword search for pathways - same AND/OR logic as
\code{\link{RunSCssGSEA}}: a character vector applies OR; a list of
character vectors applies AND within each element, OR across elements.
Case-insensitive.}

\item{pathways}{Exact pathway names to plot (alternative to
\code{search_terms}).}

\item{top_n}{\emph{Direct mode only.} When neither \code{pathways} nor
\code{search_terms} is given, plot the top N by \code{|NES|}.  Default
\code{5}.}

\item{label_levels}{\emph{RunGSEA directory mode.} Which label.by levels to
include. \code{NULL} = all.}

\item{group_levels}{\emph{RunGSEA directory mode.} Which group.by (cluster)
levels to include as pages. \code{NULL} = all.}

\item{split_levels}{\emph{RunGSEA directory mode.} Which split.by (cell type)
levels to include as pages. \code{NULL} = all.}

\item{max_cols}{\emph{RunGSEA directory mode.} Maximum number of group.by
levels (columns) per page. When the total number of groups exceeds this
value the groups are chunked across multiple pages within the same PDF.
Default \code{6}.}

\item{colors}{Named vector with \code{"pos"} (positive NES) and \code{"neg"}
(negative NES) color entries. Default red / blue.}

\item{ncol}{\emph{Direct mode only.} Number of columns in the output layout.
Default \code{1}.}

\item{output_dir}{Directory to save PDFs. \code{NULL} = no save.}

\item{object_name}{Label prefix for output file names.}

\item{subset_name}{Optional subset label.}
}
\value{
\emph{Direct mode:} a single \code{patchwork} object.
\emph{RunGSEA directory mode:} invisibly, a nested list
\code{[[lab]][[cl]][[pw]][[sp]]} of mountain-plot patchwork objects;
PDFs are written to \code{output_dir} (one per lab×cl combination).
}
\description{
Two operating modes:

\strong{Direct mode} - pass \code{fgsea_result} and \code{ranked_stats}
directly (e.g. from an ad-hoc fgsea run or a single CSV).

\strong{RunGSEA directory mode} - point to the \code{output_dir} of a
completed \code{\link{RunGSEA}} run via \code{gsea_dir}.  The function
auto-discovers all CSV files, searches pathway names with keyword search
(same AND/OR logic as \code{\link{RunSCssGSEA}}), loads the ranked AUC
vectors from the cached DE RDS files, and produces a grid layout:
\itemize{
\item \strong{One PDF per split.by level} (cell type) - each PDF may have
multiple pages when there are more groups than \code{max_cols}
\item \strong{Columns} = group.by levels (comparison groups, e.g. Male / Female)
- up to \code{max_cols} per page; additional groups wrap to new pages
\item \strong{Rows} = matched pathways (one 3-panel mountain stack per pathway)
}
}
\seealso{
\code{\link{RunGSEA}}, \code{\link{RunEnrichment}}
}
