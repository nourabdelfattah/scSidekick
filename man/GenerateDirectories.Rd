% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_directories.R
\name{GenerateDirectories}
\alias{GenerateDirectories}
\title{Create a standard project directory tree}
\usage{
GenerateDirectories(
  base_dir,
  subset = "All_Clusters",
  object_name = NULL,
  resolution = NULL,
  output_subdirs = "Featuremaps",
  extra_top = NULL,
  set_wd = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{base_dir}{Character. Project root directory. Created if absent.}

\item{subset}{Character. Name of the analysis branch under \verb{Output/}
(e.g. \code{"All_Clusters"}, \code{"Tcells"}). Default \code{"All_Clusters"}.}

\item{object_name}{Character or \code{NULL}. Project label. Stored on the
returned list (and used by downstream functions for file prefixes); does
not affect the folder structure. Default \code{NULL}.}

\item{resolution}{Numeric or \code{NULL}. Clustering resolution. Stored on the
returned list for reference only. Default \code{NULL}.}

\item{output_subdirs}{Character vector. Subfolders to create \emph{inside} the
\verb{Output/<subset>/} branch. Default \code{"Featuremaps"}. Pass \code{character(0)}
for none, or e.g. \code{c("Featuremaps", "DotPlots", "Pathways")}.}

\item{extra_top}{Character vector or \code{NULL}. Additional top-level folders to
create directly under \code{base_dir} (beyond \code{R_Objects}, \code{R_Code},
\code{Output}). Default \code{NULL}.}

\item{set_wd}{Logical. Set the working directory to \code{base_dir}? Default
\code{FALSE}.}

\item{verbose}{Logical. Message each folder as it is created? Default
\code{TRUE}.}
}
\value{
Invisibly, a named list of absolute paths:
\describe{
\item{\code{base_dir}}{the project root}
\item{\code{robj_dir}}{\code{base_dir/R_Objects}}
\item{\code{rcode_dir}}{\code{base_dir/R_Code}}
\item{\code{output_root}}{\code{base_dir/Output}}
\item{\code{output_dir}}{\verb{base_dir/Output/<subset>} - the per-analysis branch}
\item{one entry per \code{output_subdirs}}{keyed by a lower-case, snake-case
version of the folder name, e.g. \code{featuremaps}}
\item{one entry per \code{extra_top}}{keyed the same way}
\item{\code{object_name}, \code{subset}, \code{resolution}}{echoed back for convenience}
}
Use \code{output_dir} directly with \code{\link[=PrepObject]{PrepObject()}} or any plotting function.
}
\description{
Builds the conventional scSidekick analysis folder layout under \code{base_dir}
and returns every created path as a named list.  Replaces the usual pile of
\code{paste0()} + \code{dir.create()} lines at the top of an analysis script.

The tree created is:

\if{html}{\out{<div class="sourceCode">}}\preformatted{base_dir/
  R_Objects/                  # saved Seurat objects (.rds)
  R_Code/                     # analysis scripts
  Output/                     # all figures / tables
    <subset>/                 # one analysis branch (e.g. "All_Clusters")
      Featuremaps/            # (and any other `output_subdirs`)
}\if{html}{\out{</div>}}
}
\examples{
\dontrun{
dirs <- GenerateDirectories(
  base_dir    = "~/Projects/YAP_scRNAseq",
  subset      = "All_Clusters",
  object_name = "YAP project",
  resolution  = 1,
  output_subdirs = c("Featuremaps", "DotPlots", "Pathways"))

# Wire the paths straight into the rest of the workflow:
obj <- PrepObject(obj, variables = "celltype.l1",
                  output_dir = dirs$output_dir, object_name = dirs$object_name)
saveRDS(obj, file.path(dirs$robj_dir, "obj.rds"))
RunGSEA(obj, output_dir = dirs$pathways)
}

}
\seealso{
\code{\link[=PrepObject]{PrepObject()}}, \code{\link[=LoadSamplesRNA]{LoadSamplesRNA()}}
}
