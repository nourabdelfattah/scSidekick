% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/atlas_overview.R
\name{PlotAtlasWheel}
\alias{PlotAtlasWheel}
\title{Dataset Overview Wheel}
\usage{
PlotAtlasWheel(
  meta,
  group.by,
  study_by = NULL,
  patient_by = NULL,
  facet.by = NULL,
  ncol_facet = NULL,
  exclude_groups = NULL,
  normal_col = NULL,
  normal_val = NULL,
  normal_count_by = NULL,
  title = "Dataset Overview",
  cell_label = "Cells",
  show_stats = TRUE,
  colors = NULL,
  show_circles = TRUE,
  circle_size = 18,
  wedge_alpha = 0.5,
  y_inner = -1,
  y_outer = 10,
  y_text = 7,
  y_label = 11,
  y_circle = 1.5,
  y_min = -15,
  y_center_title = -5.5,
  y_center_stats = -13,
  label_size = 4,
  study_text_size = 2.2,
  study_lineheight = 0.9,
  title_size = 7,
  stats_size = 5,
  title_color = "#1e4e5f",
  output_dir = NULL,
  object_name = "",
  pdf_width = 9,
  pdf_height = 9
)
}
\arguments{
\item{meta}{A Seurat object \strong{or} a plain \code{data.frame}/\code{tibble}
of per-cell (or per-sample) metadata.}

\item{group.by}{Character.  Primary grouping column - one wedge per level
(e.g. \code{"CancerType"}).}

\item{study_by}{Character or \code{NULL}.  Column whose values are pasted
inside each wedge as a multi-line study label (e.g. \code{"Study"}).
When \code{NULL}, no inner text is drawn.}

\item{patient_by}{Character or \code{NULL}.  Column identifying individual
donors / patients - used to count unique donors in the summary statistics.}

\item{facet.by}{Character or \code{NULL}.  Metadata column to facet by -
one wheel per level (e.g. \code{"Tissue"}).  Default \code{NULL}.}

\item{ncol_facet}{Integer or \code{NULL}.  Number of wheel columns when
\code{facet.by} is used.  Auto-computed (max 2) if \code{NULL}.}

\item{exclude_groups}{Character vector or \code{NULL}.  Levels of
\code{group.by} to exclude from the wheel itself, but whose cells are
still counted in the center statistics.  Typical use:
\code{exclude_groups = "Healthy"}.}

\item{normal_col, normal_val}{Character or \code{NULL}.  Column and value
used to identify normal (non-disease) samples
(e.g. \code{normal_col = "Health"}, \code{normal_val = "Healthy"}).}

\item{normal_count_by}{Character or \code{NULL}.  Column to count distinct
normal tissue types (e.g. \code{"Organ"}).}

\item{title}{Character.  Main title text in the center of the wheel.
Supports \code{"\\n"} for line breaks.}

\item{cell_label}{Character.  Descriptor appended to the cell count
(e.g. \code{"Myeloid Cells"}).  Default \code{"Cells"}.}

\item{show_stats}{Logical.  Show cell / donor / group counts in the center.
Default \code{TRUE}.}

\item{colors}{Named character vector mapping \code{group.by} levels to
colors.  Auto-assigned if \code{NULL}.}

\item{show_circles}{Logical.  Draw a white placeholder circle at the inner
base of each wedge.  Default \code{TRUE}.}

\item{circle_size}{Numeric.  \code{size} of placeholder circles.
Default \code{18}.}

\item{wedge_alpha}{Numeric.  Transparency of the wedge fill.  Default
\code{0.5}.}

\item{y_inner}{Numeric.  Inner y edge of the wedge ring (top of the center
hole).  Default \code{-1}.}

\item{y_outer}{Numeric.  Outer y edge of the wedge ring.  Default \code{10}.}

\item{y_text}{Numeric.  y position of the study-name text inside each
wedge.  Default \code{7}.}

\item{y_label}{Numeric.  y position of the outer group-name labels.
Default \code{11}.}

\item{y_circle}{Numeric.  y position of the inner placeholder circles.
Default \code{1.5}.}

\item{y_min}{Numeric.  Bottom of the y-axis range.  Default \code{-15}.}

\item{y_center_title}{Numeric.  y position of the center title annotation.
Default \code{-5.5}.}

\item{y_center_stats}{Numeric.  y position of the center statistics.
Default \code{-13}.}

\item{label_size}{Numeric.  Font size of outer group-name labels.
Default \code{4}.}

\item{study_text_size}{Numeric.  Font size of study-name text inside
wedges.  Default \code{2.2}.}

\item{study_lineheight}{Numeric.  Line height for multi-line study labels.
Default \code{0.9}.}

\item{title_size}{Numeric.  Font size of the center title.  Default
\code{7}.}

\item{stats_size}{Numeric.  Font size of the center statistics text.
Default \code{5}.}

\item{title_color}{Character.  Color for the center title and circle
borders.  Default \code{"#1e4e5f"}.}

\item{output_dir}{Directory to save a PDF.  \code{NULL} = no save.}

\item{object_name}{Label prefix for the output filename.}

\item{pdf_width, pdf_height}{PDF dimensions per wheel in inches.  Default
\code{9 x 9}.}
}
\value{
A \code{ggplot2} or \code{patchwork} object (invisibly).
}
\description{
Creates a circular "atlas wheel" that summarises a large multi-cohort
dataset.  Each wedge represents one level of \code{group.by}
(e.g. cancer type, tissue, condition) and is annotated with the names of
contributing studies.  Summary statistics (total cells, donors, groups,
and optionally normal-tissue types) are placed in the center.

Works with both \strong{Seurat objects} and plain \code{data.frame}s.

When \code{facet.by} is supplied, one wheel is built per level, all
assembled side-by-side with \pkg{patchwork}.  The filename includes
\code{"by_{facet.by}"}.  Outer label margins are auto-expanded based on the
longest group-level name.
}
