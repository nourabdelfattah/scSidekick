% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dotplots.R
\name{SplitDotPlot}
\alias{SplitDotPlot}
\title{Dot plot split by a metadata variable with numbered group labels}
\usage{
SplitDotPlot(
  seurat_object,
  markers_df,
  gene_column = "Genes",
  gene_group_column = "CellType",
  group.by = "seurat_clusters",
  split.by = NULL,
  gene_col = NULL,
  gene_group_col = NULL,
  ClusterFeatures = FALSE,
  col.min = 0,
  col.max = 2.5,
  scale.min = 5,
  scale.max = 100,
  dot.scale = 6,
  cols = "RdBu",
  output_dir = NULL,
  file_name = NULL
)
}
\arguments{
\item{seurat_object}{A Seurat object.}

\item{markers_df}{A data frame containing at least two columns: gene names
and gene group labels.}

\item{gene_column}{Character. Column in \code{markers_df} with gene names.
Default \code{"Genes"}.}

\item{gene_group_column}{Character. Column in \code{markers_df} with gene group
labels (used for x-axis faceting). Default \code{"CellType"}.}

\item{group.by}{Character. Metadata column for the y-axis (cell groups).
Default \code{"GlobalAssignment"}.}

\item{split.by}{Character or \code{NULL}. Metadata column to split rows by.
\code{NULL} shows all groups together. Default \code{"CancerType"}.}

\item{ClusterFeatures}{Logical. Hierarchically cluster cell groups
(y-axis)? Default \code{FALSE}.}

\item{col.min, col.max}{Numeric. Color scale clamp limits for scaled average
expression. Defaults \code{0} and \code{2.5}.}

\item{scale.min, scale.max}{Numeric. Size scale clamp limits for percent
expressed. Defaults \code{5} and \code{100}.}

\item{dot.scale}{Numeric. Maximum dot radius. Default \code{6}.}

\item{cols}{Ignored (kept for API compatibility). Color palette is viridis
plasma.}

\item{output_dir}{Character or \code{NULL}. Directory to save a PDF.
\code{NULL} (default) returns the plot without saving.  When
\code{NULL}, the function walks up to the \code{output_dir} stored by
\code{\link{PrepObject}} — unless \code{AutoSavePlots = FALSE} was set
there.}

\item{file_name}{Character or \code{NULL}. Base name (no extension) for the
saved PDF. \code{NULL} (default) auto-deduces the name from the
\code{markers_df} variable name, falling back to
\code{object_name_group.by_SplitDotPlot}.}
}
\value{
A ggplot2 object (invisibly when saved to disk).
}
\description{
Generates a dot plot where dot color encodes scaled average expression and
dot size encodes percent of cells expressing each gene. Cell groups
(y-axis) are numbered and can be hierarchically clustered. Genes (x-axis)
are grouped by a gene-metadata column and faceted accordingly.
}
\details{
A temporary metadata column (\code{Temp_Interaction_Var}) is added to the
Seurat object during computation and automatically removed on exit.
}
