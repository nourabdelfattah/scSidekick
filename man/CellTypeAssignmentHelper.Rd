% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cell_annotation.R
\name{CellTypeAssignmentHelper}
\alias{CellTypeAssignmentHelper}
\title{Automated cell-type annotation with rcellmarker, scType, and SingleR}
\usage{
CellTypeAssignmentHelper(
  seurat_object,
  markers,
  cluster_column = "Cluster",
  output_dir = NULL,
  object_name = "",
  subset_name = "",
  reduction = "umap",
  cluster_colors = NULL,
  species = "human",
  run_rcellmarker = TRUE,
  run_sctype = TRUE,
  sctype_tissues = c("Immune system", "Brain"),
  sctype_assay = "RNA",
  sctype_layer = "scale.data",
  sctype_db = NULL,
  run_singler = TRUE,
  singler_ref = NULL,
  singler_col = "SingleRAssignment",
  markers_csv = NULL,
  marker_groups = NULL,
  top_n_per_cluster = 3L,
  extra_dimplot_cols = NULL,
  extra_col_colors = NULL,
  col.min = 0,
  col.max = 2,
  scale.min = 0,
  scale.max = 100
)
}
\arguments{
\item{seurat_object}{A Seurat object.}

\item{markers}{Data frame of DE results from \code{\link[presto:wilcoxauc]{presto::wilcoxauc()}}. Required
for rcellmarker annotation and for the top-marker dotplot. Expected columns:
\code{feature}, \code{group}, \code{avgExpr}, \code{avg_log2FC} (or \code{logFC}), \code{auc},
\code{pval}, \code{padj}, \code{pct_in}, \code{pct_out}.}

\item{cluster_column}{Character. Metadata column holding cluster labels.
Default \code{"Cluster"}.}

\item{output_dir}{Character. Directory for PDFs.}

\item{object_name}{Character. Prefix for PDF filenames.}

\item{subset_name}{Character. Second prefix component.}

\item{reduction}{Character. UMAP reduction name. Default \code{"umap"}.}

\item{cluster_colors}{Named character vector of colours for \code{cluster_column}
levels. \code{NULL} auto-generates from \code{Nour_pal()}.}

\item{species}{Character. \code{"human"} (default) or \code{"mouse"}. Controls:
(1) which gene column is read from the markers CSV (\code{Human} vs \code{Mouse}),
(2) the rcellmarker \code{species} argument, and
(3) which CellChatDB is loaded in downstream CellChat workflows.}

\item{run_rcellmarker}{Logical. Run rcellmarker automated annotation?
Requires the \code{rcellmarker} package. Default \code{TRUE}.}

\item{run_sctype}{Logical. Run scType automated annotation? Requires
\code{openxlsx}. Default \code{TRUE}.}

\item{sctype_tissues}{Character vector of tissue type(s) to query from the
bundled \code{ScTypeDB_full.xlsx}. Combine multiple types for broader coverage.
All valid values (from the bundled database):
\itemize{
\item \code{"Adrenal"} — adrenal gland
\item \code{"Brain"} — cerebral cortex, broad neuronal/glial types
\item \code{"Eye"} — retinal and eye-specific cell types
\item \code{"Heart"} — cardiac cell types
\item \code{"Hippocampus"} — hippocampal-specific subtypes (use with \code{"Brain"}
for combined brain data)
\item \code{"Immune system"} — broad immune cells: T, B, NK, myeloid, etc.
\item \code{"Intestine"} — intestinal epithelial and stromal types
\item \code{"Kidney"} — renal cell types
\item \code{"Liver"} — hepatocytes, Kupffer cells, stellate cells
\item \code{"Lung"} — airway epithelium, alveolar cells, immune
\item \code{"Muscle"} — skeletal and smooth muscle
\item \code{"Pancreas"} — islet and exocrine types
\item \code{"Placenta"} — trophoblast and decidual types
\item \code{"Spleen"} — splenic immune populations
\item \code{"Stomach"} — gastric epithelial types
\item \code{"Thymus"} — thymocyte and thymic stromal types
}
Recommended combinations: \code{c("Brain", "Hippocampus")} for brain snRNAseq;
\code{c("Immune system", "Spleen")} for immune-rich tissues;
\code{c("Immune system", "Brain")} for neuroinflammation datasets.}

\item{sctype_assay}{Character. Seurat assay to use for scoring.
Must contain a \code{scale.data} (scaled) layer. Common choices:
\itemize{
\item \code{"RNA"} — standard full assay (default); use \code{sctype_layer = "scale.data"}
\item \code{"sketch"} — BPCells sketch assay for large datasets; use
\code{sctype_layer = "scale.data"} after \code{ScaleData()} on the sketch
}}

\item{sctype_layer}{Character. Layer within \code{sctype_assay} that holds
scaled expression values. Default \code{"scale.data"}. Must be a scaled
(mean-centred) matrix — scType scores are not meaningful on raw counts.}

\item{sctype_db}{Character or \code{NULL}. Path to a \code{ScTypeDB_full.xlsx}
database file. \code{NULL} (default) uses the copy bundled in
\code{inst/extdata/ScTypeDB_full.xlsx}; if that is missing it is downloaded
once from the scType GitHub and cached at
\code{tools::R_user_dir("scSidekick", "cache")}.}

\item{run_singler}{Logical. Run SingleR cluster-level annotation? Requires
\code{SingleR}, \code{scuttle}, \code{celldex}, \code{SingleCellExperiment}. Default \code{TRUE}.}

\item{singler_ref}{A reference \code{SummarizedExperiment} for
\code{\link[SingleR:SingleR]{SingleR::SingleR()}}, or \code{NULL} to use \code{\link[celldex:BlueprintEncodeData]{celldex::BlueprintEncodeData()}}
(default). Choose based on tissue type and species:
\describe{
\item{\strong{Human references}:}{}
\item{\code{celldex::BlueprintEncodeData()}}{Blueprint/ENCODE bulk RNA-seq.
Broad cell types; good general-purpose starting point. \strong{Default.}}
\item{\code{celldex::HumanPrimaryCellAtlasData()}}{Human Primary Cell Atlas.
Very broad coverage across many tissues; best for highly heterogeneous
datasets with diverse cell populations.}
\item{\code{celldex::MonacoImmuneData()}}{Monaco et al. Finest immune subtype
resolution (29 populations). Best for PBMCs, spleen, or any
immune-focused study.}
\item{\code{celldex::DatabaseImmuneCellExpressionData()}}{DICE database.
Good alternative immune reference with activated/resting states.}
\item{\code{celldex::NovershternHematopoieticData()}}{Hematopoietic lineages.
Best for bone marrow or stem-cell datasets.}
\item{\strong{Mouse references}:}{}
\item{\code{celldex::ImmGenData()}}{ImmGen consortium. The most comprehensive
mouse immune cell reference (over 250 subtypes).}
\item{\code{celldex::MouseRNAseqData()}}{Broad mouse tissue types from bulk
RNA-seq. Good for non-immune mouse tissues.}
}
All celldex references are downloaded once and cached by
\code{BiocFileCache}; subsequent calls are instant.}

\item{singler_col}{Character. Name for the new metadata column storing
numbered SingleR labels (e.g. \code{"01.CD8+ T cells"}).
Default \code{"SingleRAssignment"}.}

\item{markers_csv}{Character or \code{NULL}. Path to a canonical-marker CSV.
\code{NULL} uses the CSV bundled with scSidekick (\code{inst/extdata/Markers.csv}).
Required columns: \code{Type}, \verb{Cell types}, plus \code{Human} and/or \code{Mouse}
gene symbol columns. The bundled CSV covers brain and immune markers
(see \code{marker_groups} for the full list of \code{Type} values).}

\item{marker_groups}{Named list controlling how many dotplots are produced
and which \code{Type} values from the markers CSV appear in each. One
\code{\link[=SplitDotPlot]{SplitDotPlot()}} is generated per list entry. \code{NULL} produces a single
dotplot with all markers combined.

\strong{\code{Type} values in the bundled \code{Markers.csv}:}
\itemize{
\item \code{"Brain Stroma"} — Astrocytes, Oligodendrocytes, OPC, Microglia,
Endothelial, Pericytes, Neurons (excitatory, inhibitory, dopaminergic,
glutamatergic, serotonergic), VLMC, and stem/progenitor types
\item \code{"Lymphocytes"} — T cells (naive, cytotoxic, Th1/2/17, Treg,
exhausted, MAIT, GDT), B cells, NK cells
\item \code{"Monocytes"} — CD14/CD16 monocytes, MDSCs
\item \code{"Macrophages"} — tissue macrophages
\item \code{"Dendritic cells"} — cDC1, cDC2, pDCs, LAMP3+ DCs
\item \code{"Granulocytes"} — neutrophils, basophils, mast cells
\item \code{"Bone Marrow Derived"} — BMD immune cells
\item \code{"All"} — proliferating cells (Prol)
\item \code{"Tumor"} — generic tumor marker
}

Example for brain snRNAseq with separate immune panel:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{marker_groups = list(
  "Brain"  = "Brain Stroma",
  "Immune" = c("Lymphocytes", "Monocytes", "Macrophages",
               "Dendritic cells", "Granulocytes",
               "Bone Marrow Derived", "All")
)
}\if{html}{\out{</div>}}}

\item{top_n_per_cluster}{Integer. Number of top DE genes per cluster shown
in the cluster-faceted dotplot (uses the \code{markers} data frame).
Default \code{3}.}

\item{extra_dimplot_cols}{Character vector of additional metadata columns
to include as panels in the multi-UMAP summary figure (e.g. \code{"Subclass"}
for SEAAD reference labels or \code{"Donor.ID"} for QC). Colours are
auto-generated from \code{Nour_pal()} unless supplied via \code{extra_col_colors}.}

\item{extra_col_colors}{Named list of colour vectors for \code{extra_dimplot_cols}.
Names must match \code{extra_dimplot_cols}. Example:
\code{list(Subclass = subclass_colors)}.}
}
\value{
The modified Seurat object (returned invisibly), with:
\describe{
\item{New metadata columns}{\code{scType_CellType},
\code{rcellmarker_CellType}, and/or the column named by
\code{singler_col}, depending on which methods ran.}
\item{\code{seurat_object@misc$annotation_colors}}{Named list of colour vectors for
each annotation column: \code{rcellmarker}, \code{sctype},
\code{singler}, plus any \code{extra_dimplot_cols} colours. Retrieve
with \code{seurat_object@misc$annotation_colors$sctype} etc.}
}
}
\description{
Runs up to three automated cell-type annotation methods on a Seurat object,
adds their results as new metadata columns, and produces two summary PDFs:
\enumerate{
\item An \verb{AutoAssignment umaps.pdf} with scType, rcellmarker, and cluster
UMAPs side-by-side.
\item An \verb{AutoAssignment umaps with singleR.pdf} + \verb{Assignment Helper.pdf}
combining all annotations and canonical-marker dotplots.
}
}
\section{rcellmarker}{

}

\section{scType}{

}

\section{SingleR}{

}

\section{Dotplots}{

}

\section{UMAP panels}{

}

