% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{RenameClusters}
\alias{RenameClusters}
\title{Rename numeric cluster IDs to prefixed labels}
\usage{
RenameClusters(x, prefix = "C", start = 1L)
}
\arguments{
\item{x}{A vector of numeric or character cluster IDs (e.g. \code{0, 1, 2, ...}).}

\item{prefix}{Character prefix to prepend. Default \code{"C"}.}

\item{start}{Integer. The output number assigned to the lowest cluster.
\code{start = 1} (default) maps cluster 0 → \code{"C01"}.
\code{start = 0} maps cluster 0 → \code{"C00"}.}
}
\value{
A factor with levels in cluster order.
}
\description{
Converts 0-based or 1-based integer cluster IDs (as produced by Seurat's
\code{FindClusters}) into zero-padded labels with a custom prefix, e.g.
cluster 0 → \code{"C01"}, cluster 1 → \code{"C02"}.
}
\examples{
\dontrun{
SeuratObj$Cluster <- RenameClusters(SeuratObj$seurat_clusters, prefix = "C", start = 1)
# cluster 0 → "C01", cluster 1 → "C02", ...
}
}
