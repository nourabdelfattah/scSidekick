% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pptx.R
\name{log_analysis_params}
\alias{log_analysis_params}
\title{Log Seurat analysis parameters to a JSON file}
\usage{
log_analysis_params(
  obj,
  output_dir,
  dataset = "",
  subset_name = "",
  assay_type = "scRNAseq",
  params_json = file.path(output_dir, "analysis_params.json")
)
}
\arguments{
\item{obj}{A Seurat object.}

\item{output_dir}{Character. Directory where figures are saved (and where
\code{analysis_params.json} will be written).}

\item{dataset}{Character. Human-readable dataset label
(e.g. \code{"SEAAD snRNAseq"}).}

\item{subset_name}{Character. Subset label (e.g. \code{"All_Clusters"}).}

\item{assay_type}{Character. Sequencing modality. Controls the word used for
individual observations in auto-generated legends and methods text.
Recognised values (case-sensitive):
\itemize{
\item \code{"scRNAseq"} → "single-cell RNA-seq" / "cells" (default)
\item \code{"snRNAseq"} → "single-nucleus RNA-seq" / "nuclei"
\item \code{"scATACseq"} → "single-cell ATAC-seq" / "cells"
\item \code{"scMultiome"} → "single-cell multiome" / "cells"
\item \code{"Spatial"} → "spatial transcriptomics" / "spots"
\item \code{"Visium"} → "Visium spatial transcriptomics" / "spots"
\item \code{"VisiumHD"} → "Visium HD spatial transcriptomics" / "bins"
\item \code{"Xenium"} → "Xenium in situ transcriptomics" / "cells"
}
Any other string is passed through as-is (long name) with "cells" as the
observation word.}

\item{params_json}{Character. Full path for the JSON file.  Defaults to
\code{file.path(output_dir, "analysis_params.json")}.}
}
\value{
Invisibly returns the logged parameter list.
}
\description{
Reads the command history of a Seurat object to extract key analysis
parameters (resolution, PCs, Harmony variables, cell count, cluster count)
and writes them to \code{analysis_params.json} in \code{output_dir}.  When
\code{ExtractMethods()} is available in the session the full structured summary
it returns is used; otherwise parameters are read directly from
\code{obj@commands}.
}
\details{
Call this once, after \code{RunHarmony} / \code{FindClusters} / \code{RunUMAP} are
complete.  Then call \code{\link[=create_analysis_pptx]{create_analysis_pptx()}} at the end of the Rmd.
}
