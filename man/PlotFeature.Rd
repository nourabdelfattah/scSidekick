% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_feature.R
\name{PlotFeature}
\alias{PlotFeature}
\title{Violin / box / bar plots for gene expression and numeric metadata}
\usage{
PlotFeature(
  data,
  features,
  group.by,
  split.by = NULL,
  row.by = NULL,
  exclude = NULL,
  plot_type = c("violin", "box", "both", "bar"),
  add_points = FALSE,
  point_size = 0.3,
  alpha = 0.7,
  add_stats = FALSE,
  comparisons = NULL,
  ref_group = NULL,
  hide_ns = FALSE,
  stat_alpha = 0.05,
  label_format = c("stars", "p.format"),
  ncol = NULL,
  downsample = NULL,
  colors = NULL,
  assay = "RNA",
  layer = "data",
  output_dir = NULL,
  object_name = "",
  pdf_width = NULL,
  pdf_height = NULL
)
}
\arguments{
\item{data}{A Seurat object or a plain data frame.}

\item{features}{Character vector of gene names and/or numeric metadata
column names.  Mixed vectors are supported.}

\item{group.by}{Character.  Categorical metadata column for the x-axis
(groups / conditions).}

\item{split.by}{Character or \code{NULL}.  Creates column facets within
each feature panel - one panel per level of \code{split.by}.}

\item{row.by}{Character or \code{NULL}.  Creates row facets within each
feature panel.  Combined with \code{split.by} produces a two-way
\code{facet_grid}.}

\item{exclude}{Named list of values to \strong{exclude} before plotting.
Each name is a column name and each value is a character vector of levels
to drop.  Can target \code{group.by}, \code{split.by}, \code{row.by}, or
any other metadata column.
Example: \code{list(Cognitive.Status = "Reference", Sex = "Unknown")}.}

\item{plot_type}{One of \code{"violin"} (default), \code{"box"},
\code{"both"} (violin with a thin white box overlay), or \code{"bar"}
(mean ± SE bar chart).}

\item{add_points}{Logical.  Overlay jittered individual points.
Default \code{FALSE}.}

\item{point_size}{Numeric.  Size of jittered points.  Default \code{0.3}.}

\item{alpha}{Numeric.  Fill transparency for violins / bars.  Default
\code{0.7}.}

\item{add_stats}{Logical.  Add Wilcoxon significance brackets via
\pkg{ggsignif}.  Default \code{FALSE}.}

\item{comparisons}{List of length-2 character vectors specifying pairs to
compare.  \code{NULL} (default) tests all pairwise combinations, or all
vs \code{ref_group} when that is set.}

\item{ref_group}{Character or \code{NULL}.  Reference group for
comparisons (all others tested against it).}

\item{hide_ns}{Logical.  Suppress \code{"ns"} brackets.  Default
\code{FALSE}.}

\item{stat_alpha}{Numeric.  Significance threshold used for bracket labeling
and for determining star levels (\code{*} at \code{stat_alpha}, \code{**}
at \code{stat_alpha / 5}, \code{***} at \code{stat_alpha / 50}).  Default
\code{0.05}.}

\item{label_format}{One of \code{"stars"} (default: \code{*},
\code{**}, \code{***}) or \code{"p.format"} (numeric p-value shown).}

\item{ncol}{Integer or \code{NULL}.  Columns in the patchwork assembly.
\code{NULL} = \code{min(n_features, 3)}.}

\item{downsample}{Integer or \code{NULL}.  If supplied, downsample to at
most this many cells total (balanced across \code{group.by} levels) before
plotting.  \code{NULL} (default) uses all cells.}

\item{colors}{Named character vector of fill colors for \code{group.by}
levels.  \code{NULL} auto-resolves from \code{PrepObject} (if a Seurat
object is passed) or \code{SelectColors()}.}

\item{assay}{Character.  Seurat assay to pull gene expression from.
Default \code{"RNA"}.}

\item{layer}{Character.  Assay layer / slot.  Default \code{"data"}
(log-normalized counts).}

\item{output_dir}{Character or \code{NULL}.  Save directory.  Walks up
from \code{PrepObject} when a Seurat object is passed.}

\item{object_name}{Character.  Filename prefix.  Walks up from
\code{PrepObject} when a Seurat object is passed.}

\item{pdf_width}{Numeric or \code{NULL}.  Override the auto-calculated PDF
width in inches.  \code{NULL} (default) sizes automatically from the
number of groups, splits, and features.}

\item{pdf_height}{Numeric or \code{NULL}.  Override the auto-calculated PDF
height in inches.  \code{NULL} (default) sizes automatically from the
number of row facets and features.}
}
\value{
When one feature is requested, a single \code{ggplot2} object.
When multiple features are requested, a \code{patchwork} combined plot.
The plot is also saved as a PDF when \code{output_dir} is available.
}
\description{
Plots one panel per feature - either a gene expression value pulled from
the specified assay layer or a numeric metadata column - grouped by a
categorical metadata variable on the x-axis.  Multiple features are
assembled with \pkg{patchwork}.
}
\details{
Feature type is auto-detected: gene names found in the assay are extracted
as log-normalized expression; numeric metadata columns are used directly.
Passing a \strong{categorical} metadata column raises a helpful error pointing
to the appropriate scSidekick visualization function instead.
}
\examples{
\dontrun{
# Gene expression violin plot
PlotFeature(SeuratObj, features = c("CD3E", "CD79A", "LYZ"),
  group.by = "CellType")

# Numeric metadata with split + stats, excluding a reference group
PlotFeature(SeuratObj, features = "Age.at.Death",
  group.by  = "Dementia.AD",
  split.by  = "Sex",
  exclude   = list(Cognitive.Status = "Reference"),
  add_stats = TRUE,
  plot_type = "box")

# Two-way facet: split.by columns × row.by rows
PlotFeature(SeuratObj, features = c("CD3E", "LYZ"),
  group.by = "Group",
  split.by = "Timepoint",
  row.by   = "Sex")
}
}
