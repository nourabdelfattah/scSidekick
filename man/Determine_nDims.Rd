% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dimreduction.R
\name{Determine_nDims}
\alias{Determine_nDims}
\title{Determine the optimal number of dimensions for downstream analysis}
\usage{
Determine_nDims(
  seurat_object,
  threshold_pct = 5,
  reduction = "pca",
  threshold_cum_pct = 90,
  threshold_diff_pct = 0.1,
  elbow_plot = TRUE
)
}
\arguments{
\item{seurat_object}{A Seurat object with the target reduction already
computed (e.g. via \code{RunPCA()} or \code{RunLSI()}).}

\item{threshold_pct}{Numeric. A selected dimension must contribute less than
this percentage of total variance. Default \code{5}.}

\item{reduction}{Character. Name of the reduction slot to inspect. Default
\code{"pca"}.}

\item{threshold_cum_pct}{Numeric. Cumulative variance (\%) that must be
reached before Heuristic 1 can trigger. Default \code{90}.}

\item{threshold_diff_pct}{Numeric. Minimum drop in per-dimension variance
between consecutive dimensions for Heuristic 2. Default \code{0.1}.}

\item{elbow_plot}{Logical. Return an annotated elbow plot? Default \code{TRUE}.}
}
\value{
A named list:
\describe{
\item{n_dims}{Recommended number of dimensions (integer).}
\item{cum_thresh}{Dimension selected by Heuristic 1.}
\item{elbow_thresh}{Dimension selected by Heuristic 2.}
\item{plot}{A ggplot2 elbow plot, or \code{NULL} when \code{elbow_plot = FALSE}.}
}
}
\description{
Uses two complementary heuristics to choose how many dimensions from a
linear reduction (PCA, LSI, etc.) to pass to UMAP, neighbor graphs, and
clustering.
}
\details{
\strong{Heuristic 1 - cumulative variance plateau}: finds the first dimension
where cumulative variance explained exceeds \code{threshold_cum_pct} \emph{and} the
per-dimension contribution has already dropped below \code{threshold_pct}. This
marks the point of diminishing returns.

\strong{Heuristic 2 - elbow}: finds the last dimension where the drop in
per-dimension variance between successive dimensions is still larger than
\code{threshold_diff_pct}. Beyond this point the curve has flattened.

The function returns the \strong{minimum} of the two, which is the more
conservative choice.
}
