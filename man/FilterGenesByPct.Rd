% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter_features.R
\name{FilterGenesByPct}
\alias{FilterGenesByPct}
\title{Filter genes by percent expression across groups}
\usage{
FilterGenesByPct(
  seurat_object,
  gene_list,
  group_by,
  assay = NULL,
  layer = "data",
  expr_threshold = 0,
  pct_threshold = 20,
  mode = c("any_group", "all_groups")
)
}
\arguments{
\item{seurat_object}{A Seurat object.}

\item{gene_list}{Character vector of gene names to test.}

\item{group_by}{Character. Metadata column defining the groups.}

\item{assay}{Character or \code{NULL}. Assay to pull expression from. Defaults
to \code{DefaultAssay()}.}

\item{layer}{Character. Layer/slot to use (\code{"data"}, \code{"counts"}, etc.).
Default \code{"data"}.}

\item{expr_threshold}{Numeric. A cell is considered "expressing" the gene
if its value is strictly above this threshold. Default \code{0}.}

\item{pct_threshold}{Numeric. Minimum percent (0–100) of cells in a group
that must express the gene. Default \code{20}.}

\item{mode}{Character. \code{"any_group"} (default) keeps a gene if it passes
the threshold in at least one group; \code{"all_groups"} requires every group
to pass.}
}
\value{
A named list:
\describe{
\item{\code{genes}}{Character vector of genes that passed the filter.}
\item{\code{pct_table}}{Data frame (genes × groups) of percent-expressed
values, with an extra \code{gene} column. Pass to \code{\link[=PlotPctHeatmap]{PlotPctHeatmap()}}.}
}
}
\description{
Returns the subset of \code{gene_list} where the fraction of cells expressing
each gene (above \code{expr_threshold}) meets \code{pct_threshold} in either any or
all groups. Works with Assay, Assay5, and BPCells-backed objects.
}
\seealso{
\code{\link[=PlotPctHeatmap]{PlotPctHeatmap()}}
}
