% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cnv.R
\name{RunCopyKAT}
\alias{RunCopyKAT}
\title{Run CopyKAT copy-number inference}
\usage{
RunCopyKAT(
  seurat_object,
  cell_type_col = "Assignment",
  ref_cells = NULL,
  output_dir = NULL,
  n_cores = 4L,
  LOW_DR = 0.05,
  UP_DR = 0.1,
  win.size = 25L,
  caffeinate = FALSE
)
}
\arguments{
\item{seurat_object}{A Seurat object.}

\item{cell_type_col}{Character. Metadata column used to color output plots.
Default \code{"Assignment"}.}

\item{ref_cells}{Character vector of cell-type labels to use as the diploid
reference. \code{NULL} runs in unsupervised mode (CopyKAT infers normals
automatically). Default \code{NULL}.}

\item{output_dir}{Character or \code{NULL}. Directory for CopyKAT output files
and plots. Walks up from \code{\link[=PrepObject]{PrepObject()}} when \code{NULL}.}

\item{n_cores}{Integer. Parallel threads. Default \code{4L}.}

\item{LOW_DR, UP_DR}{Numeric. Lower and upper bounds on the dropout rate
filter passed to CopyKAT. Defaults \code{0.05} / \code{0.1}.}

\item{win.size}{Integer. Genomic window size (number of genes) for CNV
smoothing. Default \code{25}.}

\item{caffeinate}{Logical. Prevent the machine from sleeping. Default \code{FALSE}.}
}
\value{
The input Seurat object with \code{copykat_prediction} and \code{copykat_score}
added to metadata.
}
\description{
Fast, reference-optional CNV calling using the CopyKAT Bayesian model.
Useful as a quick first-pass or to validate \code{\link[=RunInferCNV]{RunInferCNV()}} results.
Adds \code{copykat_prediction} (\code{"aneuploid"} / \code{"diploid"} / \code{"not.defined"})
and \code{copykat_score} to Seurat metadata.
}
\seealso{
\code{\link[=RunInferCNV]{RunInferCNV()}}
}
