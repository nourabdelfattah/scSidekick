% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_samples.R
\name{PlotQCMetrics}
\alias{PlotQCMetrics}
\title{Compute QC metrics and generate pre-filtration QC plots}
\usage{
PlotQCMetrics(
  seurat_object,
  sample_col = "Sample",
  sample_levels = NULL,
  group_col = NULL,
  output_dir,
  object_name = "",
  subset_name = "",
  species = "human",
  mt_pattern = NULL,
  ribo_pattern = NULL,
  assay = "RNA",
  sample_colors = NULL,
  count_vlines = c(500, 50000),
  feature_vlines = c(300, 80000),
  mito_vline = 0.1,
  complexity_vline = 0.8
)
}
\arguments{
\item{seurat_object}{Seurat object (merged, unfiltered).}

\item{sample_col}{Character. Metadata column holding sample identity.
Default \code{"Sample"}.}

\item{sample_levels}{Character vector or \code{NULL}. Desired factor level order
for \code{sample_col}. \code{NULL} leaves the current order unchanged.}

\item{group_col}{Character or \code{NULL}. Column used to colour bars in the
doublet and miQC barplots (e.g. \code{"orig.ident"}). \code{NULL} uses
\code{sample_col}.}

\item{output_dir}{Character. Directory for PDF output.}

\item{object_name, subset_name}{Character. Prefix components for PDF filenames.}

\item{species}{Character. Controls the default mitochondrial and ribosomal
gene patterns. One of:
\itemize{
\item \code{"human"} - \code{mt_pattern = "^MT-"}, \code{ribo_pattern = "^RP[LS]"}
\item \code{"mouse"} - \code{mt_pattern = "^mt-"}, \code{ribo_pattern = "^Rp[ls]"}
}}

\item{mt_pattern}{Character or \code{NULL}. Override the mitochondrial gene
regex. \code{NULL} derives from \code{species}.}

\item{ribo_pattern}{Character or \code{NULL}. Override the ribosomal gene regex.
\code{NULL} derives from \code{species}.}

\item{assay}{Character. Seurat assay for \code{PercentageFeatureSet()}.
Default \code{"RNA"}.}

\item{sample_colors}{Named character vector mapping \code{sample_col} levels to
colours. \code{NULL} auto-generates from \code{Nour_pal()}.}

\item{count_vlines}{Numeric vector. Vertical guide lines on the nCount_RNA
density plot. Default \code{c(500, 50000)}.}

\item{feature_vlines}{Numeric vector. Guide lines on the nFeature_RNA
density plot. Default \code{c(300, 80000)}.}

\item{mito_vline}{Numeric. Guide line on the mitoRatio density plot.
Default \code{0.1}.}

\item{complexity_vline}{Numeric. Guide line on the log10GenesPerUMI
density plot. Default \code{0.8}.}
}
\value{
The modified Seurat object with \code{mitoRatio}, \code{riboRatio}, and
\code{log10GenesPerUMI} added to \verb{@meta.data} (invisibly).
}
\description{
Adds \code{mitoRatio}, \code{riboRatio}, and \code{log10GenesPerUMI} to the object, then
saves four PDFs:
\enumerate{
\item \strong{Doublet barplot} - cells per sample coloured by \code{DoubletStatus}
(only if that column exists from \code{SCP::db_scDblFinder}).
\item \strong{miQC keep barplot} - cells per sample coloured by \code{miQC.keep}
(only if that column exists from \code{SeuratWrappers::RunMiQC}).
\item \strong{QC density + violin plots} - four metrics side-by-side with
user-defined cutoff guidelines.
\item \strong{Cell count barplot} - cells per sample before any filtering.
}
}
