% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz_de.R
\name{PlotEnrichment}
\alias{PlotEnrichment}
\title{Over-Representation Analysis Visualization}
\usage{
PlotEnrichment(
  enrichment_df,
  term_column = NULL,
  padj_column = NULL,
  gene_count_column = NULL,
  gene_ratio_column = NULL,
  group_column = NULL,
  top_n = 20,
  plot_type = "dot",
  x_by = "gene_ratio",
  color_by = "padj",
  colors = c("#B40426", "#FDDBC7", "#D1E5F0", "#3B4CC0"),
  max_term_length = 50,
  output_dir = NULL,
  object_name = "",
  subset_name = ""
)
}
\arguments{
\item{enrichment_df}{Data frame with ORA results, or a character path to a
CSV file.}

\item{term_column}{Column with term/pathway names. Auto-detected from common
names (\code{"term"}, \code{"Term"}, \code{"Description"}, etc.).}

\item{padj_column}{Adjusted p-value column. Auto-detected.}

\item{gene_count_column}{Column with overlap gene count. Auto-detected.}

\item{gene_ratio_column}{Column with gene ratio or fold enrichment.
Auto-detected; computed from \code{gene_count_column / background} if
absent.}

\item{group_column}{Optional column to facet by (e.g. cluster or condition).
Produces one panel per group.}

\item{top_n}{Maximum number of terms to show per group. Default \code{20}.}

\item{plot_type}{One of \code{"dot"} (default), \code{"bar"}, or
\code{"lollipop"}.}

\item{x_by}{What drives the x-axis (or bar length). One of
\code{"gene_ratio"} (default), \code{"gene_count"}, or \code{"-log10padj"}.}

\item{color_by}{What drives fill/color. One of \code{"padj"} (default) or
\code{"gene_count"}.}

\item{colors}{Character vector of 2–5 colours for the gradient (low padj →
high padj, i.e. significant → not). Default diverging red–blue.}

\item{max_term_length}{Maximum characters for term label before wrapping.
Default \code{50}.}

\item{output_dir}{Directory to save a PDF. \code{NULL} = no save.}

\item{object_name}{Label prefix for output file names.}

\item{subset_name}{Optional subset label.}
}
\value{
A \code{ggplot2} or \code{patchwork} object.
}
\description{
Produces bar, dot, or lollipop plots from ORA enrichment results (enrichR,
clusterProfiler, gprofiler2, or any table with term names and p-values).
Column names are auto-detected for the most common output formats; override
with the \code{*_column} parameters when needed.
}
