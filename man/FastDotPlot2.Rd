% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dotplots.R
\name{FastDotPlot2}
\alias{FastDotPlot2}
\title{FastDotPlot with optional EnrichR enrichment header}
\usage{
FastDotPlot2(
  seurat_object,
  assay = "RNA",
  features = NULL,
  pattern = NULL,
  group.by = "seurat_clusters",
  split.by = NULL,
  ClusterGroups = FALSE,
  ClusterGenes = TRUE,
  k_genes = 1,
  min.pct.exp = 15,
  col.min = 0,
  col.max = 2.5,
  scale.min = 5,
  scale.max = 100,
  dot.scale = 6,
  RunEnrichR = TRUE,
  EnrichR_DB = "CellMarker_2024",
  TopN_Enrich = 3
)
}
\arguments{
\item{seurat_object}{A Seurat object.}

\item{assay}{Character. Assay to use. Default \code{"RNA"}.}

\item{features}{Character vector of gene names, or \code{NULL} if using
\code{pattern}.}

\item{pattern}{Character regex. All matching genes in \code{rownames(seurat_object)} are
used. Ignored if \code{features} is provided.}

\item{group.by}{Character. Metadata column for y-axis. Default
\code{"GlobalAssignment"}.}

\item{split.by}{Character or \code{NULL}. Metadata column for row facets.}

\item{ClusterGroups}{Logical. Cluster y-axis groups? Default \code{FALSE}.}

\item{ClusterGenes}{Logical. Cluster genes on x-axis? Default \code{TRUE}.}

\item{k_genes}{Integer. Number of gene clusters (panels) to cut the
dendrogram into. \code{1} = no cutting (single panel). Default \code{1}.}

\item{min.pct.exp}{Numeric. Minimum percent expression threshold.
Default \code{15}.}

\item{col.min, col.max}{Numeric. Color clamp limits. Defaults \code{0}, \code{2.5}.}

\item{scale.min, scale.max}{Numeric. Size clamp limits. Defaults \code{5}, \code{100}.}

\item{dot.scale}{Numeric. Max dot radius. Default \code{6}.}

\item{RunEnrichR}{Logical. Run EnrichR enrichment? Default \code{TRUE}.}

\item{EnrichR_DB}{Character. EnrichR database name. Default
\code{"CellMarker_2024"}.}

\item{TopN_Enrich}{Integer. Number of top terms to show per pattern.
Default \code{3}.}
}
\value{
A named list:
\describe{
\item{plot}{The combined (enrichment + dot plot) or standalone dot plot.}
\item{dotplot}{The standalone dot plot ggplot2 object.}
\item{enrich_plot}{The enrichment header ggplot2 object, or \code{NULL}.}
\item{patterns}{Named list mapping each pattern label to its gene vector.}
\item{filtered_genes}{Data frame with columns \code{features.plot} and
\code{Gene_Cluster} for every gene that passed \code{min.pct.exp}.}
\item{enrich_table}{Raw EnrichR results data frame, or \code{NULL}.}
}
}
\description{
Extends \code{\link[=FastDotPlot]{FastDotPlot()}} with an optional enrichment annotation panel
aligned above the dot plot columns. When \code{RunEnrichR = TRUE}, the top
\code{TopN_Enrich} significant terms from the specified EnrichR database are
displayed as a text header above each gene-cluster panel.
}
\details{
\strong{Bug fixes vs. original script:}
\itemize{
\item Gene-cluster strip labels are shown even when enrichment is
disabled or fails (they were previously always hidden).
\item The filtered gene list and per-pattern gene assignments are always
returned in the output list regardless of enrichment status.
}
}
