% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz_composition.R
\name{PlotRose}
\alias{PlotRose}
\title{Rose (Polar Bar) Composition Chart}
\usage{
PlotRose(
  data,
  stat.by,
  group.by,
  donor.by = NULL,
  facet.by = NULL,
  ncol_facet = NULL,
  stat_type = "percent",
  colors = NULL,
  alpha = 0.85,
  label = TRUE,
  label_size = 2.5,
  min_label_pct = 2,
  keep_empty = FALSE,
  output_dir = NULL,
  object_name = "",
  pdf_width = 7,
  pdf_height = 7
)
}
\arguments{
\item{data}{A Seurat object or a plain \code{data.frame}/\code{tibble}.}

\item{stat.by}{Character. Variable whose levels become the fill colour
(e.g. \code{"Sample"} or \code{"CERAD"}).}

\item{group.by}{Character. Variable that defines the x-axis petals before
polar transformation (e.g. \code{"Assignment"} or \code{"BraakStage"}).}

\item{donor.by}{Character or \code{NULL}.  Column identifying the donor /
patient.  When supplied, metadata is first deduplicated to one row per
donor so petal sizes reflect \emph{donor counts} - appropriate for
patient-level neuropathological or clinical variables.  Default
\code{NULL}.}

\item{facet.by}{Character or \code{NULL}.  Metadata column to facet by -
one rose panel per level.  Default \code{NULL}.}

\item{ncol_facet}{Integer or \code{NULL}.  Number of facet columns when
\code{facet.by} is used.  Auto-computed (max 3) if \code{NULL}.}

\item{stat_type}{Character.  \code{"percent"} (default) plots the fraction
of \code{stat.by} within each \code{group.by} level;
\code{"count"} plots raw counts.}

\item{colors}{Named character vector for \code{stat.by} levels.
Auto-assigned if \code{NULL}.}

\item{alpha}{Numeric. Fill transparency. Default \code{0.85}.}

\item{label}{Logical. Show proportion/count labels on each segment.
Default \code{TRUE}.}

\item{label_size}{Numeric. Label text size. Default \code{2.5}.}

\item{min_label_pct}{Numeric. Minimum percentage below which labels are
suppressed (avoids clutter in tiny slices). Default \code{2}.}

\item{keep_empty}{Logical. Include \code{group.by × stat.by} combinations
with zero observations. Default \code{FALSE}.}

\item{output_dir}{Directory to save a PDF.  \code{NULL} = no save.}

\item{object_name}{Label prefix for the output filename.}

\item{pdf_width, pdf_height}{PDF dimensions per panel in inches.  Scales
with number of facet panels.  Default \code{7 x 7}.}
}
\value{
A \code{ggplot2} object (invisibly).
}
\description{
Visualises the composition of \code{stat.by} within each level of
\code{group.by} as a polar bar (rose) chart.  Each petal of the rose
corresponds to a \code{group.by} level; the petal is split by colour into
\code{stat.by} proportions or counts.

When \code{facet.by} is supplied, native \code{facet_wrap} is used so each
panel shows its own rose.  Plot margins are auto-expanded based on the
longest \code{group.by} label.
}
