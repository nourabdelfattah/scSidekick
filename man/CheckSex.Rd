% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_sex.R
\name{CheckSex}
\alias{CheckSex}
\title{Verify sample sex annotation using sex-specific gene expression}
\usage{
CheckSex(
  seurat_object,
  sex_col = "sex",
  sample_col = "Sample",
  cell_type_col = NULL,
  species = NULL,
  female_genes = NULL,
  male_genes = NULL,
  assay = NULL,
  layer = "data",
  noise_floor = 0.05,
  output_dir = NULL,
  plot = TRUE
)
}
\arguments{
\item{seurat_object}{A Seurat object.}

\item{sex_col}{Character. Metadata column containing annotated sex.
Accepted values: \code{"F"/"M"}, \code{"Female"/"Male"}, \code{"female"/"male"},
\code{"1"/"2"}. Default \code{"sex"}.}

\item{sample_col}{Character. Metadata column identifying donors/samples.
Used for per-donor pseudobulk aggregation. \code{NULL} skips sample-level
analysis and returns only per-cell scores for visualisation.
Default \code{"Sample"}.}

\item{cell_type_col}{Character or \code{NULL}. When supplied, a per-cell-type
pseudobulk table is computed in addition to the sample-level table —
useful for distinguishing BMT/chimerism (cell-type-specific mismatch)
from labeling errors (all cell types mismatched). Default \code{NULL}.}

\item{species}{Character. \code{"mouse"}, \code{"human"}, or \code{"zebrafish"}. Auto-
detected from \code{\link[=AnnotateFeatures]{AnnotateFeatures()}} if previously run. Zebrafish has no
universal sex-gene defaults — supply \code{female_genes} and \code{male_genes}.}

\item{female_genes}{Character vector or \code{NULL}. Override default female
marker genes. When \code{NULL} the package defaults are used (XIST/Tsix with
automatic X-paralog fallback).}

\item{male_genes}{Character vector or \code{NULL}. Override default male marker
genes. When \code{NULL} the package defaults are used.}

\item{assay}{Character or \code{NULL}. Assay to pull expression from. Defaults
to \code{DefaultAssay()}.}

\item{layer}{Character. Layer within the assay. Default \code{"data"}
(log-normalized counts). Falls back to \code{"counts"} automatically if
\code{"data"} is not present (BPCells objects). Only ~4–8 sex genes are
materialised into memory — near-instantaneous even on million-cell objects.}

\item{noise_floor}{Numeric. Pseudobulk mean expression threshold that
separates "completely absent" from "present" at the sample level.
Default \code{0.05}. Because this is applied to the sample mean (averaged over
hundreds–thousands of cells), alignment-artifact signal from X–Y paralog
multi-mapping (typically 0.001–0.01 in pseudobulk space) is well below
this threshold, while true sex-gene expression (0.2–2.0) is well above.
Raise to \code{0.1} if you see "Undetermined" on low-depth samples; lower to
\code{0.02} for very high-depth data.}

\item{output_dir}{Character or \code{NULL}. Directory for saved figures and CSV
summary tables. Walks up from \code{\link[=PrepObject]{PrepObject()}} when \code{NULL}.}

\item{plot}{Logical. Generate and save figures. Default \code{TRUE}.}
}
\value{
The input Seurat object with the following metadata columns added:
\describe{
\item{\code{predicted_sex}}{Sample-level sex call (\code{"Female"} / \code{"Male"} /
\code{"Ambiguous"} / \code{"Undetermined"}), propagated identically to all
cells within each sample.}
\item{\code{sex_score_f}}{Per-cell mean female-marker expression (for
visualisation; not used for the mismatch decision).}
\item{\code{sex_score_m}}{Per-cell mean male-marker expression.}
\item{\code{sex_ratio}}{Per-cell \code{log2(female_score + 0.01) / (male_score + 0.01)}. Positive = female signal, negative = male signal. Used for
the continuous UMAP ratio plot.}
\item{\code{sex_mismatch}}{Sample-level mismatch flag (\code{TRUE}/\code{FALSE}/\code{NA}),
propagated to all cells in the sample. \code{TRUE} only when expected
markers are completely absent \strong{and} opposite markers are present.}
}
}
\description{
Compares each sample's annotated sex against pseudobulk expression of
sex-specific genes. Sex is called at the \strong{sample level} (averaged across
all cells in the donor), not per cell. This avoids false positives from
single-cell dropout: a female cell may fail to capture XIST in any given
sequencing run, but a truly male sample will have zero XIST across all cells.
}
\details{
A sample is flagged only when \strong{both} of the following hold:
\enumerate{
\item The expected sex marker is \strong{completely absent} at pseudobulk level
(bulk mean ≤ \code{noise_floor}).
\item The opposite-sex marker is \strong{clearly present} at pseudobulk level
(bulk mean > \code{noise_floor}).
}

This strict two-condition rule avoids flags on:
\itemize{
\item Dropout (expected marker absent but no opposite signal either).
\item X–Y paralog multi-mapping artifacts: DDX3X/DDX3Y, KDM6A/UTY, and
KDM5C/KDM5D share 70–85\\% sequence identity, so single cells can show
spurious Y-gene signal from misaligned X reads. At the pseudobulk level
this artifact is tiny (scattered reads in a few cells) and stays well
below \code{noise_floor}.
}

\strong{Female-scoring modes} (auto-selected):
\itemize{
\item \strong{Primary} (XIST-based): uses \code{XIST}/\code{Xist}, the X-inactivation signal.
Most specific when available.
\item \strong{Fallback} (X-paralog dosage): uses \code{DDX3X}, \code{KDM6A}, \code{KDM5C} (or
mouse equivalents). Triggered when XIST/Tsix are absent from the panel
(common when lncRNAs are filtered or targeted panels are used). In this
mode mismatch is called using sample-level prediction rather than the
noise_floor threshold, because X-paralogs are expressed in males at lower
(not zero) levels.
}

When \code{cell_type_col} is supplied, a per-cell-type pseudobulk breakdown is
also computed. A sample flagged only in specific cell types (e.g., immune
cells) suggests BMT/chimerism; one flagged across all cell types suggests a
labeling error.
}
\seealso{
\code{\link[=AnnotateFeatures]{AnnotateFeatures()}}
}
