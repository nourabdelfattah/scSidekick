% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial_plots.R
\name{PlotSpatialFeaturePlots}
\alias{PlotSpatialFeaturePlots}
\alias{GenerateSpatialFeatureMaps}
\title{Batch spatial feature plots for genes and/or metadata columns}
\usage{
PlotSpatialFeaturePlots(
  seurat_object,
  features,
  layout_method = "auto",
  row.by = NULL,
  images_to_plot = NULL,
  remove_outliers = FALSE,
  outlier_prob = 0.01,
  size_override = NULL,
  colors = c("#053061", "#2166AC", "#D1E5F0", "#FDDBC7", "#F4A582", "#D6604D", "#B2182B",
    "#67001F"),
  calcptsizesc = 100,
  legend.side = "right",
  legend.direction = "vertical",
  join_plots = FALSE,
  join_nrow = NULL,
  join_ncol = NULL,
  output_dir = NULL,
  object_name = "",
  subset_name = ""
)

GenerateSpatialFeatureMaps(
  seurat_object,
  features,
  layout_method = "auto",
  row.by = NULL,
  images_to_plot = NULL,
  remove_outliers = FALSE,
  outlier_prob = 0.01,
  size_override = NULL,
  colors = c("#053061", "#2166AC", "#D1E5F0", "#FDDBC7", "#F4A582", "#D6604D", "#B2182B",
    "#67001F"),
  calcptsizesc = 100,
  legend.side = "right",
  legend.direction = "vertical",
  join_plots = FALSE,
  join_nrow = NULL,
  join_ncol = NULL,
  output_dir = NULL,
  object_name = "",
  subset_name = ""
)
}
\arguments{
\item{seurat_object}{A Seurat object with spatial images.}

\item{features}{Character vector of gene names or numeric metadata
column names.}

\item{layout_method}{Character. \code{"auto"} (default) or \code{"metadata"}.}

\item{row.by}{Character or \code{NULL}. Slide-level metadata column for
grouping images into rows (required when \code{layout_method = "metadata"}).}

\item{images_to_plot}{Character vector or \code{NULL}. Subset of image names
to include. \code{NULL} uses all images.}

\item{remove_outliers}{Logical. Crop outlier cells before plotting.
Default \code{FALSE}.}

\item{outlier_prob}{Numeric. Quantile to trim when \code{remove_outliers = TRUE}.
Default \code{0.01}.}

\item{size_override}{Scalar or named numeric list. A plain number (e.g.
\code{size_override = 1.5}) applies to all images; a named list applies
per image. Overrides auto-sizing.}

\item{colors}{Character vector. Gradient colors from low to high.}

\item{calcptsizesc}{Numeric. Scaling constant for auto point-size
calculation. Default \code{100}.}

\item{legend.side}{Character. Where to place the legend panel relative to
the image grid: \code{"right"} (default), \code{"left"}, \code{"bottom"},
or \code{"top"}.}

\item{legend.direction}{Character. Internal layout of legend entries:
\code{"vertical"} (default, entries stacked) or \code{"horizontal"}
(entries arranged in rows). Also controls colorbar orientation.}

\item{join_plots}{Logical. When \code{features} has more than one gene, combine
all per-feature figures into a single page/PDF instead of one each.
Default \code{FALSE}.}

\item{join_nrow, join_ncol}{Integer or \code{NULL}. Grid layout for \code{join_plots}.
\code{NULL} auto-arranges into a near-square grid.}

\item{output_dir}{Character or \code{NULL}. Output directory for PDFs. Returns
last plot when \code{NULL}.}

\item{object_name}{Character. Label appended to PDF filenames. Default \code{""}.}

\item{subset_name}{Character. Optional second label appended to PDF
filenames after \code{object_name}. Default \code{""}.}
}
\value{
Invisibly returns the last plot when \code{output_dir = NULL}. When
\code{join_plots = TRUE}, invisibly returns the combined tiled figure.
}
\description{
Loops over \code{features} (gene names or numeric metadata columns) and
produces \code{\link[Seurat:SpatialPlot]{Seurat::SpatialFeaturePlot()}} panels for each image in the
object. A shared color scale (0 → max) is applied across all images for
the same feature. Panels are arranged in a grid and a shared legend is
attached.
}
\details{
Supports two layout modes:
\describe{
\item{\code{"auto"}}{Automatic 1- or 2-row grid.}
\item{\code{"metadata"}}{Groups images into rows based on a slide-level
metadata variable (\code{row.by}). The variable must have a single
unique value per image.}
}
}
\section{Functions}{
\itemize{
\item \code{GenerateSpatialFeatureMaps()}: Long-form alias.

}}
