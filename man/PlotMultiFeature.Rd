% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz_multifeature.R
\name{PlotMultiFeature}
\alias{PlotMultiFeature}
\title{Plot multiple features on a single embedding}
\usage{
PlotMultiFeature(
  seurat_object,
  features,
  colors = NULL,
  null_color = "grey70",
  threshold = 0.0125,
  plot_mode = c("gene", "intersection", "scale"),
  pct_cutoff = 0.3,
  intersection_label = "Co-expressing",
  assay = "RNA",
  layer = "data",
  reduction = "umap",
  pt.size = 0.5,
  alpha = 0.9,
  shape = 16,
  title = NULL,
  subtitle = NULL,
  legend_title = "Gene",
  show_legend = TRUE,
  output_dir = NULL,
  object_name = "",
  subset_name = ""
)
}
\arguments{
\item{seurat_object}{A Seurat object (v3 or v5).}

\item{features}{Character vector of gene names to visualise.}

\item{colors}{Named or unnamed character vector of colors (one per gene).
Auto-filled from \code{Nour_pal("all")} for missing entries. If
\code{NULL}, the full \code{Nour_pal("all")} palette is used.}

\item{null_color}{Color for cells that express no gene above threshold.
Default \code{"grey70"}.}

\item{threshold}{Numeric scalar, or named numeric vector (names = gene
names), giving the minimum expression value for a cell to be considered
positive.  Default \code{0.0125}.}

\item{plot_mode}{One of \code{"gene"} (default), \code{"intersection"},
or \code{"scale"}.}

\item{pct_cutoff}{For \code{plot_mode = "intersection"}: the fraction of
the gene list a cell must express to count as co-expressing.  Default
\code{0.3} (i.e. ≥30 \\% of genes).}

\item{intersection_label}{Label shown in the legend for co-expressing cells.
Default \code{"Co-expressing"}.}

\item{assay}{Assay to pull expression from.  Default \code{"RNA"}.}

\item{layer}{Layer / slot to pull (Seurat v5 layer name or v3 slot name).
Default \code{"data"}.}

\item{reduction}{Dimensional reduction to use for coordinates.  Default
\code{"umap"}.}

\item{pt.size}{Point size.  Default \code{0.5}.}

\item{alpha}{Point transparency \code{[0, 1]}.  Default \code{0.9}.}

\item{shape}{ggplot2 point shape integer.  Default \code{16} (filled
circle).}

\item{title}{Plot title.  Default \code{NULL}.}

\item{subtitle}{Plot subtitle.  Default \code{NULL}.}

\item{legend_title}{Legend title.  Default \code{"Gene"}.}

\item{show_legend}{Logical; show color legend?  Default \code{TRUE}.}

\item{output_dir}{Directory to save a PDF.  If \code{NULL} (default) the
plot is printed to the active graphics device.}

\item{object_name}{Optional prefix used when building the output file name.}

\item{subset_name}{Optional second prefix for the output file name.}
}
\value{
A \code{ggplot} object (invisibly when \code{output_dir} is set).
}
\description{
Overlays several genes onto one UMAP / tSNE / PCA plot, coloring cells by
which gene they express above a threshold.  Three modes are available:
}
\details{
\describe{
\item{\strong{"gene"} (default)}{Each cell is colored by the last gene
in \code{features} that it expresses above threshold (later genes
overwrite earlier ones for multi-expressing cells).  Cells below all
thresholds are drawn in \code{null_color}.}
\item{\strong{"intersection"}}{Cells that express at least
\code{pct_cutoff} × n_genes genes are highlighted as co-expressing.
All others are \code{null_color}.}
\item{\strong{"scale"}}{Continuous color gradient showing expression
level of a single gene (\code{features[1]} only).}
}
}
