% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/session_logger.R
\name{start_session_logger}
\alias{start_session_logger}
\title{Start the scSidekick passive session logger}
\usage{
start_session_logger(log_root = NULL, quiet = FALSE)
}
\arguments{
\item{log_root}{Character.  Parent directory for log files.
Defaults to \verb{~/.R/session_logs}.}

\item{quiet}{Logical.  If \code{FALSE} (default) prints a startup message showing
the log file path.}
}
\value{
Invisibly returns the path to the log file.
}
\description{
Attaches an R task callback that silently logs every top-level expression
evaluated in the session, together with its status (OK / ERR), timestamp,
and working directory.  ggplot2 figures are auto-saved as PNGs whenever a
new plot is detected; base-R graphics are captured on \code{plot.new}.
}
\details{
Logs are written to \verb{<log_root>/<session_id>_session.log} and plots to
\verb{<log_root>/<session_id>_plots/}.  Call \code{\link[=summarize_r_session]{summarize_r_session()}} afterwards
to generate a markdown report and a clean reconstructed R script.

\strong{Recommended usage:} add the following line to your \verb{~/.Rprofile} so the
logger starts automatically in every new R session:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{if (requireNamespace("scSidekick", quietly = TRUE)) scSidekick::start_session_logger()
}\if{html}{\out{</div>}}
}
\seealso{
\code{\link[=summarize_r_session]{summarize_r_session()}}
}
