# R package build artefacts
*.Rcheck/
*.tar.gz
*.zip
*.so
*.dll
*.o
# RStudio
.Rproj.user/
.Rhistory
.RData
.Ruserdata

# macOS
.DS_Store
__MACOSX/
# Dropbox sync artefacts
.dropbox
Icon
# Test / temp outputs
*.pdf
*.pptx
Rplots.pdf

# Local data caches — regenerated by data-raw/make_readme_figures.R.
# Large and/or private (bmcite_processed.rds, seaad_meta_small.rds);
# keep the .R script tracked, ignore the cached objects.
data-raw/*.rds
# Compiled vignettes (built by R CMD check / pkgdown)
inst/doc/
vignettes/*.html
vignettes/*.R
